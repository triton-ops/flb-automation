# NJM-67687 — [FLB v1] FLB - Functional - Verify Backup to the Onboard Repository

- **Source TC:** NJM-67687 (Xray, FLB/Share, Critical P2, label flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67687, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (Onboard repo id 2 present on nbr-84)

## Objective
Verify FLB can protect a folder of mixed file types on a physical machine to the **Onboard
repository** and produce a valid recovery point containing all the selected file types
(spec: Onboard/Local is a supported FLB destination; folder-level selection).

## Fixtures used (from test-data)
- Source: windows-src **PM-3** (`Windown`, `C:\TestData_ForFLB`) — mixed types (.pdf/.docx/.xlsx/.jpg/.mp4/…)
- Repository: **Onboard repository** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** from `test-data/job-templates/flb_job.template.json`
- Job name: `AUTO_FLB_NJM-67687`

## Preconditions
- [ ] Source PM-3 present & OK — **R1** (`PhysicalDiscovery.getDiscoveryItems`)
- [ ] Onboard repo id 2 online — **R3**
- [ ] `C:\TestData_ForFLB` present with mixed file types (test-data.md §1)
- [ ] **BLOCKED guard:** if PM-3 not OK or repo 2 absent → STOP, report BLOCKED(env).

## Steps
### Step 1 — Build the job — R4c
- **Action:** load canonical template → SourceBuilder `sourceVid=PM-3`, `targetName=Windown`;
  MappingBuilder `mappings=[{NORMAL, "C:/TestData_ForFLB/Folder_test1", FOLDER}]` (a folder with
  mixed types); RepositoryBuilder `targetStorageVid=BACKUP_REPOSITORY-2`; name `AUTO_FLB_NJM-67687`;
  ValidationBuilder → `saveJob`.
- **Expect:** `{result: OK, jobId: <n>}` (new independent job; job 25 not touched).
### Step 2 — Run — R5
- **Action:** `run [{"runType":"ALL","jobIds":[<jobId>]}]` · **Expect:** async accepted.
### Step 3 — Poll — R6
- **Expect:** terminal `lrState: OK` (`lrVmOk≥1`, `lrVmFailed 0`).
### Step 4 — Verify — R7
- **Expect:** ≥1 savepoint; FLR `createSession→getState ACTIVE→list` shows the selected folder with
  **all its file types**; optional EXPORT→SHA-256 vs `manifest-windows.sha256`.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record job id.

## Expected final verdict
PASS if the job reaches `lrState:OK` and the recovery point contains every selected file type.
