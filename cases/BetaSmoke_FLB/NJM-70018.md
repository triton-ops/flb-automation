# NJM-70018 — [FLB v1] FLB - Functional - Verify Full Workflow (Create, Run, Recover, Delete Job)

- **Source TC:** NJM-70018 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-70018, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE

## Objective
Verify the full FLB job lifecycle end-to-end: create a job, run it successfully, recover selected
files via FLR, then delete the job — confirming the job disappears from the Jobs list while its
recovery points remain in the repository until separately managed.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB\ft_code`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-70018`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select `C:/TestData_ForFLB/ft_code`
  (`FOLDER`), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-70018` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK` ("Last run successful"); recovery point created.
### Step 4 — Verify recovery — R7 · FLR `list`/EXPORT confirms recovered files match source content.
### Step 5 — Delete the job — **R9** `remove [<jobId>, false]` (this IS the TC's step, not just
  cleanup) · **Expect:** `{result:"OK"}`; a subsequent `getJob [<jobId>]` returns no job / not-found.

## Cleanup
- Job deletion is the final test step itself (Step 5 above) — nothing further to clean up on PASS.
- On FAIL (job creation/run/recover failed before deletion) — leave the job in place and record its id.

## Expected final verdict
PASS if the job is created, runs to `lrState:OK`, files recover correctly via FLR, and the job is
successfully removed (`{result:"OK"}`) afterward.
