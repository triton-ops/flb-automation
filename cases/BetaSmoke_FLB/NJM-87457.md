# NJM-87457 — [FLB v1] Recovery from Tape - Verify Recovery from a Full Recovery Point

- **Source TC:** NJM-87457 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-87457, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ⚠️ PARTIAL (upgraded from BLOCKED, 2026-07-08) — tape library `VLT_Tape`
  (`TAPE_LIBRARY-1`, `state:OK`) + media pool `test1` now exist on nbr-84 (see
  `test-data/environment.md`). Requires first completing NJM-70168 (BCJ to tape) to actually get a
  full recovery point onto tape — this TC depends on that artifact existing. The tape-target job
  shape and the from-tape recovery flow are both uncalibrated; needs live introspection first.

## Objective
Verify recovery of a full FLB recovery point from tape back into a disk repository, and that the
recovered data is browsable/present in the target repository.

## Fixtures used
- Depends on NJM-70168 having produced a full recovery point on `VLT_Tape`/`test1` first
- Repository (recovery target): **Onboard** (`BACKUP_REPOSITORY-2`)

## Preconditions
- [ ] A full recovery point already exists on tape (run NJM-70168 first if not).
- [ ] **BLOCKED guard:** if no tape-based recovery point exists and NJM-70168 hasn't been run,
  BLOCKED(env) — do not fabricate a tape recovery point.

## Steps
### Step 1 — Confirm a full RP exists on `VLT_Tape` (via Activities/tape inventory, or by having
  just completed NJM-70168).
### Step 2 — Introspect the tape-recovery RPC shape (likely a variant of `JobManagement`/tape
  restore flow — uncalibrated, confirm live).
### Step 3 — Recover the full RP to Onboard; verify the data is present and browsable
  (FLR `list` or direct repo inspection).
### Step 4 — SHA-256 vs manifest to confirm content integrity survived the tape round-trip.

## Cleanup
- Remove any recovered/restored artifacts created solely for this test.

## Expected final verdict
PASS if the full RP recovers from tape intact and matches source checksums. BLOCKED(env) if no
tape-based recovery point exists yet (run NJM-70168 first).
