# NJM-123180 — [FLB v1] FLR from FLB - Functional - Verify Recovery from an Immutable Wasabi Backup

- **Source TC:** NJM-123180 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123180, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (upgraded from BLOCKED, 2026-07-08) — **`Wasabi-immutable`**
  (`BACKUP_REPOSITORY-17`, `objectLockSupported:true`) was added and its import issue resolved.
  SMTP delivery is confirmed configured and working (`Settings → Notifications`, `smtp.gmail.com`,
  live test-email sent successfully 2026-07-08). Both prerequisites are now met. Open item (as
  with every other immutability-dependent case in this batch): whether the retention option
  actually produces `isImmutableSavepoint:true` hasn't been live-tested on this repo yet.

## Objective
Verify an FLB recovery point stored in an immutable (Object Lock) Wasabi repository can be
recovered and emailed as a ZIP attachment, with content matching source and the Object Lock
remaining intact.

## Fixtures used
- Source: windows-src **PM-3** (a small fixture folder, e.g. `ft_pdf`)
- Repository: **Wasabi-immutable** (`BACKUP_REPOSITORY-17`)
- Job build: **R4c** + `options.retentionPolicy.retentionMode="RULESET"` (introspect shape first)
- Job name: `AUTO_FLB_NJM-123180`
- Email recipient: use the already-configured notification address (`Settings → Notifications`) —
  do not hardcode a new address

## Preconditions
- [ ] PM-3 present & OK — **R1**; `Wasabi-immutable` repo online — **R3**; SMTP relay confirmed
  working (already verified).
- [ ] **BLOCKED guard:** if the immutable-retention shape is rejected, or the savepoint doesn't
  show `isImmutableSavepoint:true`, stop and report BLOCKED/FAIL.

## Steps
### Step 1 — Introspect — `mcp__nbr__describe_method nbr-84 JobManagement saveJob`.
### Step 2 — Build — R4c: `sourceVid=PM-3`, select fixture folder, `retentionPolicy.retentionMode="RULESET"`,
  `targetStorageVid=BACKUP_REPOSITORY-17`, name `AUTO_FLB_NJM-123180` → `saveJob`.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 5 — Verify immutability — `getSavepoints`; **Expect:** `isImmutableSavepoint:true`.
### Step 6 — Recover — FLR `recover` with `recoveryType:"FORWARD"` (email) to the configured
  recipient; confirm the ZIP attachment arrives and its contents match source checksums.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — leave artifacts; record job name/id.

## Expected final verdict
PASS if `lrState:OK`, savepoint shows `isImmutableSavepoint:true`, and the emailed ZIP's contents
match checksums. FAIL (not BLOCKED) if immutability doesn't actually engage or the email fails to
deliver.
