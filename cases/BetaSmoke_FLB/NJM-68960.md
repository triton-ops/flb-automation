# NJM-68960 — [FLB v1] FLB - Functional - Verify Retrieved Folders are Correctly Backed Up

- **Source TC:** NJM-68960 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68960, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE

## Objective
Verify a selected **nested folder tree** (files at multiple levels) is backed up with its full
hierarchy and content intact, and recovers matching the source exactly (Xray: select the root
folder of a nested structure, run the job, recover, and compare layout + content to source).

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB\Folder_test1`, 33-file nested example set)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68960`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select `C:/TestData_ForFLB/Folder_test1`
  (root of the nested tree) as `FOLDER`, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-68960` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK` ("Last run was successful").
### Step 4 — Verify — R7 · FLR `list` recursively confirms the full hierarchy (all subfolders and
  files) is present; EXPORT→SHA-256 vs `manifests/manifest-windows.sha256` for the tree (layout +
  content match source exactly).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK` and the recovered folder tree matches the source hierarchy and checksums.
