# NJM-70168 — [FLB v1] BCJ to Tape - Verify End-to-End Backup Copy to Tape Workflow

- **Source TC:** NJM-70168 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-70168, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ⚠️ PARTIAL (upgraded from BLOCKED, 2026-07-08) — a tape library (`VLT_Tape`,
  `TAPE_LIBRARY-1`, state `OK`) and media pool (`test1`, id `1`, linked) now exist on nbr-84 — see
  `test-data/environment.md`. The hardware/media-pool blocker is resolved. Not yet calibrated:
  the exact `JobDto` shape for a Backup Copy job targeting **tape** (vs. the disk-repo shape
  already proven in R4d) — likely `targetStorageType:"TAPE"` plus a media-pool/library reference,
  but unconfirmed. `describe_method` + a live dry-run needed before this is fully RUNNABLE.

## Objective
Verify an end-to-end Backup Copy Job workflow that copies an FLB job's recovery points to a tape
media pool, and that the copy is catalogued correctly in Director's tape inventory.

## Fixtures used
- Source: an existing FLB backup object on Onboard
- Tape library: **VLT_Tape** (`TAPE_LIBRARY-1`); media pool: **test1** (id `1`)
- Job build: **R4d** pattern, adapted for a tape target — shape uncalibrated, introspect first
- Job name: `AUTO_FLB_NJM-70168`

## Preconditions
- [ ] Source `BACKUP_OBJECT` present; tape library `state:OK` and media pool linked — **R3**-equivalent.
- [ ] Check individual cartridge readiness via `TapeCartridgeManagement.getCartridges` before the
  first run (exact filter shape not yet calibrated — experiment live).
- [ ] **BLOCKED guard:** if `saveJob` rejects a tape-targeted job shape entirely (not just an
  unexpected error, but the whole job type/target combination), report BLOCKED — do not force an
  incorrect payload.

## Steps
### Step 1 — Introspect — `mcp__nbr__describe_method nbr-84 JobManagement saveJob`; check cartridge
  state via `TapeCartridgeManagement`.
### Step 2 — Build — adapt the R4d Backup Copy shape for a tape target (likely
  `targetStorageType:"TAPE"` + a media-pool/library reference — confirm exact fields live), name
  `AUTO_FLB_NJM-70168` → `saveJob`.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 5 — Verify — confirm the copy is catalogued in Director's tape inventory
  (`TapeCartridgeManagement`/Activities) matching the source backup.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — leave artifacts; record job name/id.

## Expected final verdict
PASS if `lrState:OK` and the tape catalogue reflects the copied recovery point. BLOCKED(env) only
if the tape-targeted job shape is fundamentally rejected by `saveJob` (not merely unfamiliar) —
otherwise report FAIL with the exact error.
