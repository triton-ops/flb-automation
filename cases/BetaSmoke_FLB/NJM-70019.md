# NJM-70019 — [FLB v1] FLB - Functional - Verify Use Case: Create and Run a Basic File/Folder Backup Job (UC1 & UC2)

- **Source TC:** NJM-70019 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-70019, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE

## Objective
Verify UC1 & UC2: a user can create a basic FLB job selecting specific files and folders, run it,
and the recovery point contains exactly those selected files and folders.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB\Folder_test2` + `C:\TestData_ForFLB\ft_audio\sample_audio.mp3`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-70019`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder with a mixed selection — one `FOLDER`
  (`C:/TestData_ForFLB/Folder_test2`) + one `FILE`
  (`C:/TestData_ForFLB/ft_audio/sample_audio.mp3`), `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-70019` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) — this is the "Finish and Run" step of UC1&UC2.
### Step 3 — Poll — R6 · **Expect:** `lrState:OK` ("Last run was successful").
### Step 4 — Verify — R7 · FLR `list` confirms the recovery point contains exactly the selected
  folder and file.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if the job is created, runs automatically to `lrState:OK`, and the recovery point contains
the selected files and folders.
