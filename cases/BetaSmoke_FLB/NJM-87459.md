# NJM-87459 — [FLB v1] Recovery from Tape - Verify Recovery from an Incremental Recovery Point

- **Source TC:** NJM-87459 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-87459, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ⚠️ PARTIAL (upgraded from BLOCKED, 2026-07-08) — tape library `VLT_Tape` +
  media pool `test1` now exist on nbr-84. Requires a full RP **and** a subsequent incremental RP
  already on tape (run NJM-70168 twice — full then incremental — first). Tape-target job shape
  and from-tape recovery flow both uncalibrated; needs live introspection first.

## Objective
Verify recovery of a specific incremental FLB recovery point from tape, confirming it is
recovered and browsable for file-level restore.

## Fixtures used
- Depends on a full RP + incremental RP already existing on `VLT_Tape`/`test1`
- Repository (recovery target): **Onboard** (`BACKUP_REPOSITORY-2`)

## Preconditions
- [ ] A full RP + incremental RP already exist on tape (run the BC job twice via NJM-70168's
  pattern if not).
- [ ] **BLOCKED guard:** if no incremental tape-based recovery point exists, BLOCKED(env).

## Steps
### Step 1 — Confirm a full RP + incremental RP exist on `VLT_Tape` (Activities/tape inventory).
### Step 2 — Introspect the tape-recovery RPC shape (uncalibrated, confirm live).
### Step 3 — Recover the incremental RP specifically (not the full) to Onboard; verify browsable
  for file-level restore.
### Step 4 — SHA-256 vs manifest to confirm content integrity.

## Cleanup
- Remove any recovered/restored artifacts created solely for this test.

## Expected final verdict
PASS if the incremental RP recovers from tape intact, is browsable, and matches source checksums.
BLOCKED(env) if no incremental tape-based recovery point exists yet.
