# NJM-182427 — [FLB v3] FLB Job Wizard - Step 3 Exclusion - Exclude all .pdf files [UC3] (FLB-12)

- **Source TC:** NJM-182427 (Xray, FLB/Share, Blocker P1, NJM-67219, flb-v3)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-182427, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ⚠️ PARTIAL — `test-data/job-templates/flb_job.template.json` has
  `options.enabledSourceItemsExclusion` + `options.sourceItemsExclusion` fields (and a separate
  `objects[0].excludedItems` field), but **neither field's exclusion-rule shape (e.g. an
  extension-pattern rule for `*.pdf`) is calibrated** in `recipes/file-backup-recipes.md` — per
  CLAUDE.md rule 2, `describe_method` must confirm the live shape before the first write.

## Objective
Verify an FLB job's Step-3 Exclusion rule (extension = `*.pdf`) causes all `.pdf` files under the
selected source folder to be skipped on backup, while all non-`.pdf` files in the same folder are
still backed up.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB\Folder_test1` — contains `.pdf` alongside
  many non-`.pdf` types: mp4/mkv/gif/mp3/odp/ods/ogg/ppt/svg/tiff/wav/webp/xls/xlsx/xml/ico/json/doc/odt/rtf)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + exclusion fields (uncalibrated — introspect first)
- Job name: `AUTO_FLB_NJM-182427`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if `describe_method`/a dry-run `saveJob` rejects the exclusion-rule shape
  (spec drift / undocumented format), STOP and report BLOCKED — do not guess a shape and silently
  create a job that doesn't actually exclude anything.

## Steps
### Step 1 — Introspect — `mcp__nbr__describe_method nbr-84 JobManagement saveJob` (confirm the
  exact shape of `options.sourceItemsExclusion` / `objects[0].excludedItems` for an
  extension-pattern rule before first use this session).
### Step 2 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select
  `C:/TestData_ForFLB/Folder_test1` (`FOLDER`), set the exclusion rule (extension `*.pdf`) per the
  confirmed shape from Step 1, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-182427` → `saveJob`.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 5 — Verify — R7 · FLR `list` on the recovery point confirms **zero** `.pdf` files are
  present, and all non-`.pdf` files from `Folder_test1` **are** present (compare file-name set
  against `manifests/manifest-windows.sha256`, filtered to `Folder_test1/`).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK`, no `.pdf` file appears anywhere in the recovery point, and every non-`.pdf`
file from the selected folder does. If Step 1's introspection reveals the exclusion feature isn't
exposed the way assumed, stop and report BLOCKED with the actual method spec, don't force it.
