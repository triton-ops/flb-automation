# NJM-122653 — [FLB v1] FLB Job Wizard - Validation - Ensure 'Next' Button is Enabled After Item Selection

- **Source TC:** NJM-122653 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-122653, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (upgraded from PARTIAL, 2026-07-08) — `browser/checks/check_njm_122653.py`
  now exists (adapted from `check_njm_122652.py`, inverted assertion) and **PASSED live**: ticking
  one item + Apply cleared the "No item(s) selected" note, and clicking Next correctly advanced to
  "2. Inclusion".

## Objective
Verify that once at least one item (volume/folder/file) is ticked in the Source step's "Select
Items" dialog and Applied, the wizard's "Next" button is enabled and advances to the Destination
step without a validation error.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-122653`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — UI assertion — `cd browser && python checks/check_njm_122653.py`: select machine,
  open Select Items, tick `ft_generic` folder, Apply → **Expect:** "No item(s) selected" note
  clears, Next enables, clicking Next advances to "2. Inclusion". **VERIFIED PASS 2026-07-08.**
### Step 2 — Build (RPC confirmation that the server also accepts a job with this selection) —
  R4c: `sourceVid=PM-3`, MappingBuilder select `C:/TestData_ForFLB/ft_generic` (`FOLDER`),
  `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-122653` → `saveJob`.
- **Expect:** `{OK, jobId}`.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if the UI check passes (verified) AND the RPC-built job with a non-empty selection saves and
runs to `lrState:OK`.
