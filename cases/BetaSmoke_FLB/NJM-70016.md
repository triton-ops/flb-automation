# NJM-70016 — [FLB v2] FLB - Functional - Verify User Story: Backup Specific Individual Files (US4)

- **Source TC:** NJM-70016 (Xray, FLB/Share, Blocker P1, flb-v2)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-70016, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE

## Objective
Verify US4: a user can back up **specific individual files** (not whole folders/volumes) due to
storage constraints, the job succeeds, and the recovery point contains **only** the selected
files — unselected files in the same folder are excluded.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB\ft_pdf\sample_pdf.pdf`,
  `C:\TestData_ForFLB\ft_excel\sample_excel.xlsx` — two individual files, same/different folders)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-70016`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder with TWO `FILE` mappings only (no
  `FOLDER` mappings): `C:/TestData_ForFLB/ft_pdf/sample_pdf.pdf`,
  `C:/TestData_ForFLB/ft_excel/sample_excel.xlsx`, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-70016` → `saveJob`.
- **Expect:** `{OK, jobId}`; `objects[0].mappings` has exactly 2 entries, both `sourceIdentifierType:FILE`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Verify — R7 · FLR `list` on the recovery point shows **only** the 2 selected files —
  no sibling files from `ft_pdf/`/`ft_excel/` are present.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK` and the recovery point contains exactly the 2 selected files and no others.
