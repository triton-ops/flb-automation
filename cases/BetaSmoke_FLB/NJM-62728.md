# NJM-62728 — [FLB v1] FLB Job Wizard - Functional - Verify Selection of Various Item Types (Folders, Files, Volumes) for Backup

- **Source TC:** NJM-62728 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-62728, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (upgraded from PARTIAL, 2026-07-08) — `browser/checks/check_njm_62728.py`
  now exists and its live run **PASSES under the corrected interpretation**: ticking the VOLUME
  (`Local Disk (C:)`) registered a count of 1; subsequently ticking a FOLDER and then a FILE — both
  *nested inside that same already-ticked volume* — correctly left the count unchanged at 1
  (`Selected for Physical Machine: 1` throughout), rather than increasing to 2 and 3.
  > **Correction:** the count staying at 1 was initially misread as two failed assertions ("FOLDER
  > tick is additive" / "FILE tick is additive" — both expected `!=`, which is the wrong
  > expectation). Per the same overlap logic corrected on NJM-122651: ticking a volume already
  > covers everything under it, so ticking a folder/file *already inside that volume* is a
  > redundant, overlapping selection — the count correctly does not increase. The script's
  > assertions were rewritten to reflect this (folder/file ticks nested in an already-ticked
  > volume should leave the count unchanged, not increase it); the underlying raw data (1, 1, 1)
  > is unchanged from the original run, only its interpretation was corrected — not re-run.
  > This does **not** test three mutually-independent items (this single-disk machine only
  > exposes one volume in the picker, so a non-overlapping volume+folder+file combination isn't
  > constructible here) — see recipes.md R4 / NJM-122651 for confirmation that VOLUME selection
  > and non-overlapping FOLDER+FILE selection each independently work correctly.

## Objective
Verify all three selectable item types on the FLB job wizard's Source step — a **volume**, a
**folder**, and an individual **file** — can each be added to the backup selection, confirming
the wizard supports mixed item-type selection (spec: item granularity down to volume/folder/file).

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB`) or linux-src **PM-2** (`/TestData_ForFLB`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-62728`

## Preconditions
- [ ] PM-3 (or PM-2) present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if source/repo not OK → BLOCKED(env).

## Steps
### Step 1 — UI dialog check — `cd browser && python checks/check_njm_62728.py`: tick the VOLUME
  (`Local Disk (C:)`), then a FOLDER and a FILE nested inside it. **VERIFIED PASS 2026-07-08**
  (reinterpreted per the correction above): volume tick registers count 1; nested folder/file
  ticks correctly leave the count unchanged (already covered by the volume selection).
### Step 2 — Build — R4c: `sourceVid=PM-3`, MappingBuilder with two **non-overlapping** mappings —
  one `FOLDER` (`C:/TestData_ForFLB/Folder_test1`) + one `FILE` from a different top-level folder
  (`C:/TestData_ForFLB/ft_pdf/sample_pdf.pdf`) — a VOLUME-scoped RPC mapping is not attempted here
  (VOLUME is confirmed working at the UI layer per Step 1/NJM-122651, but its
  `sourceIdentifierType` value for a direct `saveJob` call remains uncalibrated —
  `describe_method` first if needed); `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-62728` → `saveJob`.
- **Expect:** `{OK, jobId}`; `objects[0].mappings` contains both entries with correct
  `sourceIdentifierType`.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 5 — Verify — R7 · FLR `list` confirms **both** `Folder_test1` (with all its files) and
  `ft_pdf/sample_pdf.pdf` are present in the recovery point, each independently and completely
  (per the non-overlapping-mapping pattern verified working on NJM-122651).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if the UI check passes (verified) AND the RPC-built job with non-overlapping FOLDER+FILE
mappings reaches `lrState:OK` with both items fully present in the recovery point. VOLUME
selection itself is confirmed working at the UI layer (Step 1); a dedicated RPC-level volume
mapping is a documented follow-up, not required for this TC's PASS.
