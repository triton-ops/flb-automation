# NJM-182423 — [FLB v3] FLB - Recovery - Recover selected files/folders to original location (Recover to Source) [UC4]

- **Source TC:** NJM-182423 (Xray, FLB/Share, Blocker P1, NJM-67219, flb-v3)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-182423, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (upgraded from BLOCKED, 2026-07-08) — the user has explicitly
  authorized executing the destructive **Overwrite** behavior for this specific run, **scoped to a
  disposable, dedicated test file/folder created solely for this TC — never the shared
  `TestData_ForFLB` fixture** other runbooks' checksum manifests depend on. The underlying
  mechanism (`recoveryType:"ORIGINAL"`) is confirmed working on a **Windows physical source**
  (PM-3) — see `recipes/file-backup-recipes.md` R7 (proven via the Rename overwrite-behavior;
  Overwrite itself has not been executed before, this run is the first). This authorization is
  per-run and scoped to this disposable fixture only — it does not generalize to running Overwrite
  against the shared fixture or in any other case.

## Objective
Verify selected files/folders from an FLB recovery point can be recovered back to their original
source path with an overwrite behavior, restoring content exactly to where it came from.

## Fixtures used
- Source: windows-src **PM-3** — a **dedicated, disposable file/folder created specifically for
  this TC** under `C:\TestData_ForFLB\` (e.g. `AUTO_FLB_NJM-182423_disposable\`), NOT any file
  reused by other runbooks' manifests. Seed it via `mcp__remoting__winrm_run` with known content
  before backing it up.
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-182423`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] Disposable test file/folder created and its content/checksum recorded BEFORE backup (so the
  post-overwrite recovery can be verified against a known baseline).
- [ ] **BLOCKED guard:** if the disposable fixture cannot be created in isolation from
  `TestData_ForFLB`'s shared tree, STOP — do not proceed against shared content under any
  circumstance, authorization notwithstanding.

## Steps
### Step 1 — Seed — create the disposable test file/folder on PM-3 via `winrm_run`, record its
  SHA-256.
### Step 2 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select the disposable file/folder,
  `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-182423` → `saveJob`.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 5 — Modify the disposable fixture on PM-3 (e.g. overwrite its content locally with
  different bytes) so the recovery's effect is observable — this simulates "data needs restoring,"
  the actual scenario this TC represents.
### Step 6 — Recover — FLR `createSession`/`list`, then `recover` with `recoveryType:"ORIGINAL"`
  + Overwrite behavior = **Overwrite the original item if such item exists** — the destructive
  path, authorized for this disposable fixture only.
### Step 7 — Verify — via `winrm_run`, confirm the disposable fixture's content now matches the
  Step-1 baseline checksum exactly (proving the overwrite-recovery restored it correctly) and that
  no `-recovered`-suffixed sibling was created (Overwrite, unlike Rename, replaces in place).

## Cleanup
- Delete the disposable test file/folder from PM-3 via `winrm_run` regardless of PASS/FAIL (it was
  created solely for this run).
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep the job + record, still delete the
  disposable fixture from the source since it's not shared with any other case.

## Expected final verdict
PASS if `lrState:OK` and the disposable fixture's post-recovery content exactly matches the
pre-modification baseline checksum, confirming Overwrite genuinely restores original content in
place.
