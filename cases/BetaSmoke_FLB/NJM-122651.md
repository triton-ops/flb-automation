# NJM-122651 — [FLB v1] FLB Job Wizard - Functional - Verify Selection of All Item Types (Volumes, Folders, Files)

- **Source TC:** NJM-122651 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-122651, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE — fully executed 2026-07-08, verdict **PASS**.
  `browser/checks/check_njm_122651.py` **PASSED live** (UI dialog-transition assertions, including
  confirming VOLUME selection genuinely works via the UI — resolving the "VOLUME not yet tested"
  gap in `recipes/file-backup-recipes.md` R4). The RPC build→run→verify execution (Steps 2–5)
  **also PASSED** once corrected to a non-overlapping selection (see history note below).
  > **Correction history:** a first attempt (job 42) mapped a FOLDER and a FILE *inside that same
  > folder* (`Folder_test3` + `Folder_test3/file-sample_1MB.doc`) and only 1 of the folder's 21
  > files came back in the recovery point — initially logged as a "Product Bug." That test design
  > was wrong: selecting a folder already implies all files within it, so also mapping a file
  > *inside* it is a redundant, overlapping selection, not a genuine "folder + independent file"
  > case. Re-run (job 43) with a non-overlapping selection — `Folder_test3` (FOLDER) +
  > `ft_pdf/sample_pdf.pdf` (FILE, a different top-level folder) — passed cleanly: all 21 files
  > appeared under `Folder_test3` and `sample_pdf.pdf` appeared under `ft_pdf`. **The "Product
  > Bug" finding is retracted** — it was an invalid overlapping-mapping test design on my part,
  > not a real NBR defect. (Whether overlapping folder+file mappings behave correctly is now an
  > *open question*, not a confirmed bug — untested by this corrected run.)

## Objective
Verify the FLB job wizard's Source step supports selecting a volume, then a folder, then a
sub-folder, then an individual file — each replacing/adding to the prior selection correctly —
and that the resulting job runs successfully with the final (folder+subfolder+file) selection.

> Note: `TestData_ForFLB`'s documented top-level folders are flat/leaf (no genuine nested
> subfolders — see `test-data/test-data.md`), so the live check represents "sub-folder" as a
> second, separately-drilled folder+file rather than true nesting. This doesn't weaken the
> volume/folder/file tick-transition assertions, which are the TC's actual mechanism under test.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-122651`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — UI wizard walkthrough — `cd browser && python checks/check_njm_122651.py`: tick
  volume → untick → tick a folder → drill + tick a file, verifying the selected-items count
  transitions correctly at every step. **VERIFIED PASS 2026-07-08** (all 4 assertions, including
  volume tick/untick).
### Step 2 — Build — R4c: `sourceVid=PM-3`, MappingBuilder with one `FOLDER`
  (`C:/TestData_ForFLB/Folder_test3`) + one `FILE` from a **different, non-overlapping** folder
  (`C:/TestData_ForFLB/ft_pdf/sample_pdf.pdf`), `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-122651` → `saveJob`. **EXECUTED 2026-07-08:** job 43, `{result:"OK", jobId:43}`.
### Step 3 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`. **CONFIRMED.**
### Step 5 — Verify — R7 · FLR `list` on `BACKUP_OBJECT-69` (savepoint 369, `consumed:40816223`
  bytes). **RESULT: PASS** — `TestData_ForFLB` root shows both `Folder_test3` and `ft_pdf` as
  separate entries; `Folder_test3` contains **all 21 files** (`totalCount:21`, matching
  `test-data/test-data.md` exactly); `ft_pdf` contains `sample_pdf.pdf` (5800 bytes, matches
  source). Both mappings honored fully and independently.

## Cleanup
- **PASS — cleaned up.** Job 43 removed (`JobManagement.remove [43, false]`); confirmed gone via
  `getJobId("AUTO_FLB_NJM-122651")` → empty. (The earlier invalid job 42 was also removed when its
  flawed test design was identified, before the corrected run.)

## Expected final verdict
**PASS.** Both the UI-dialog check (Step 1) and the RPC end-to-end execution (Steps 2–5) passed.
An earlier attempt with an invalid overlapping FOLDER+FILE mapping (file nested inside the same
folder already selected) produced a misleading partial-backup result, initially misreported as a
product defect — that was a test-design error on the automation's part (Automation Bug), not a
genuine NBR issue, and has been retracted per the correction history above. Whether truly
overlapping folder+file mappings behave correctly remains untested and is a legitimate follow-up,
not a confirmed defect.
