# NJM-70307 — [FLB v1] FLR from FLB - Functional - Verify End-to-End Recovery via FLR Wizard

- **Source TC:** NJM-70307 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-70307, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE

## Objective
Verify an end-to-end FLR recovery: select at least one file and one folder from an existing FLB
recovery point and recover them to a CIFS share, confirming the recovered content matches the
source backup exactly.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB\ft_ppt` + `C:\TestData_ForFLB\ft_ppt\sample_ppt.pptx`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Recovery destination: CIFS share `win-fs3` (`\\10.10.15.3\SharedData`) — see `test-data/environment.md`
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-70307`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**; CIFS share on `win-fs3` reachable.
- [ ] **BLOCKED guard:** if PM-3/repo/CIFS share not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select `C:/TestData_ForFLB/ft_ppt`
  (`FOLDER`), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-70307` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Verify via FLR — R7 · `createSession` → `list` (confirms both a file and a folder are
  present in the tree) → `recover` with `recoveryType:"EXPORT", location:"CIFS",
  pathToTheShare:"\\10.10.15.3\SharedData"` → mount the share via remoting, extract the
  `Recovered-items-*.zip`, SHA-256 vs `manifests/manifest-windows.sha256`.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`, plus remove the exported zip/temp files from the
  CIFS share. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK` and the CIFS-recovered files/folder match the source backup content exactly.
