# NJM-123178 — [FLB v1] FLR from FLB - Functional - Verify Recovery from an Immutable Azure Blob Backup

- **Source TC:** NJM-123178 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123178, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (upgraded from BLOCKED, 2026-07-08) — **`Azure_Immutable`**
  (`BACKUP_REPOSITORY-12`, `objectLockSupported:true`) was added to nbr-84. Recovery target is an
  NFS share (non-destructive). Open item: whether NBR actually produces an *immutable* savepoint
  here hasn't been live-tested yet (`backupImmutabilitySupport` still `false` — capability present,
  not yet exercised).

## Objective
Verify an FLB recovery point stored in an immutable Azure Blob repository can be recovered to an
NFS share, matching source content, with the Azure immutability policy remaining intact.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB\ft_pdf` or another small fixture folder)
- Repository: **Azure_Immutable** (`BACKUP_REPOSITORY-12`) — added 2026-07-08
- Job build: **R4c** + `options.retentionPolicy.retentionMode="RULESET"` (introspect shape first)
- Job name: `AUTO_FLB_NJM-123178`

## Preconditions
- [ ] PM-3 present & OK — **R1**; `Azure_Immutable` repo online — **R3**.
- [ ] **BLOCKED guard:** if the immutable-retention option is rejected by `saveJob`, or the
  savepoint doesn't show `isImmutableSavepoint:true`, stop and report BLOCKED/FAIL.

## Steps
### Step 1 — Introspect — `mcp__nbr__describe_method nbr-84 JobManagement saveJob`.
### Step 2 — Build — R4c: `sourceVid=PM-3`, select fixture folder, `retentionPolicy.retentionMode="RULESET"`,
  `targetStorageVid=BACKUP_REPOSITORY-12`, name `AUTO_FLB_NJM-123178` → `saveJob`.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 5 — Verify immutability — `getSavepoints`; **Expect:** `isImmutableSavepoint:true`.
### Step 6 — Recover — FLR `recover` with `recoveryType:"EXPORT"` to an NFS share; SHA-256 vs manifest.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]` (immutable savepoint may resist removal until lock
  expires — expected). On FAIL/BLOCKED — leave artifacts; record job name/id.

## Expected final verdict
PASS if `lrState:OK`, savepoint shows `isImmutableSavepoint:true`, and NFS-exported copy matches
checksums. FAIL (not BLOCKED) if immutability doesn't actually engage.
