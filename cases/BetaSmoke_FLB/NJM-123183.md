# NJM-123183 — [FLB v1] FLR from FLB - Functional - Verify Recovery from an Immutable Backblaze B2 Backup Copy

- **Source TC:** NJM-123183 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123183, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (upgraded from BLOCKED, 2026-07-08) — title says Backblaze B2, body
  says Wasabi (same title/body inconsistency seen elsewhere in this batch). This runbook follows
  the title's Backblaze intent: **`BlackBlaze_Immutable`** (`BACKUP_REPOSITORY-13`,
  `objectLockSupported:true`) now exists. Backup Copy works
  (`recipes/file-backup-recipes.md` R4d, fixed `hvType:"VMWARE"`); recovery to a CIFS share is
  non-destructive. Open item: whether an immutable savepoint is actually produced hasn't been
  live-tested.

## Objective
Verify an FLB recovery point copied (via Backup Copy Job) into an immutable cloud repository can
be recovered to a CIFS share, matching source content, with the Object Lock remaining intact.

## Fixtures used
- Source: an existing FLB backup object on Onboard
- Repository: **BlackBlaze_Immutable** (`BACKUP_REPOSITORY-13`) — added 2026-07-08
- Job build: **R4d** — `backup_copy_job.template.json` (`hvType:"VMWARE"` fixed,
  `targetStorageVid=BACKUP_REPOSITORY-13`) + `retentionPolicy.retentionMode="RULESET"`
- Job name: `AUTO_FLB_NJM-123183`

## Preconditions
- [ ] Source `BACKUP_OBJECT` present; `BlackBlaze_Immutable` repo online.
- [ ] **BLOCKED guard:** if the immutable-retention shape is rejected, or the savepoint doesn't
  show `isImmutableSavepoint:true`, stop and report BLOCKED/FAIL.

## Steps
### Step 1 — Introspect — `mcp__nbr__describe_method nbr-84 JobManagement saveJob`.
### Step 2 — Build — R4d: source `BACKUP_OBJECT-<id>`, `targetStorageVid=BACKUP_REPOSITORY-13`,
  `retentionPolicy.retentionMode="RULESET"`, name `AUTO_FLB_NJM-123183` → `saveJob`.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`, `lrVmOk:1`.
### Step 5 — Verify immutability — `getSavepoints`; **Expect:** `isImmutableSavepoint:true`.
### Step 6 — Recover — FLR `recover` with `recoveryType:"EXPORT"` to a CIFS share; SHA-256 vs manifest.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — leave artifacts; record job name/id.

## Expected final verdict
PASS if `lrState:OK`, savepoint shows `isImmutableSavepoint:true`, and CIFS-exported copy matches
checksums. FAIL (not BLOCKED) if immutability doesn't actually engage. (Note: title/body disagree
on target repo type — Backblaze vs. Wasabi — flagged for Jira author follow-up; this run uses
Backblaze.)
