# NJM-70013 — [FLB v1] FLB - Functional - Verify User Story: Backup Folders (US1)

- **Source TC:** NJM-70013 (Xray, FLB/Share, Blocker P1, flb-v1; linked closed defects NJM-101274, NJM-102015)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-70013, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE

## Objective
Verify US1: a user can back up **specific folder(s)** (folder scope — NOT the whole machine/all
volumes), the job succeeds, and the recovery point contains **only** the selected folder(s) and
no other machine paths/volumes.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-70013`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**; folders exist under `C:\TestData_ForFLB`.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select ONLY specific folder(s), e.g.
  `mappings=[{NORMAL,"C:/TestData_ForFLB/Folder_test1",FOLDER}]` (not machine root / not all volumes),
  `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-70013` → `saveJob`.
- **Expect:** `{OK, jobId}`; only the chosen folder(s) in `objects[0].mappings` (count ≥1).
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK` ("Last run successful").
### Step 4 — Verify scope — R7 · FLR `list` at root shows **only** the selected folder(s), no other
  machine paths / no other volumes; scope is folder-level (smaller than full machine).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK` and the recovery point contains exactly the selected folder(s) and nothing else.
