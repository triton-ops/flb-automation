# NJM-122670 — [FLB v2] FLB Job Wizard - Source Step (UI) - Verify 'Apply' Button in 'Select Items' Dialog

- **Source TC:** NJM-122670 (Xray, FLB/Share, Blocker P1, flb-v2)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-122670, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (upgraded from PARTIAL, 2026-07-08) — `browser/checks/check_njm_122670.py`
  now exists and **PASSED live**: ticking exactly one folder and clicking Apply produced
  "Selected for Physical Machine: 1" (both before Apply and on reopening the picker afterward),
  and the Source step's note correctly read "1 item(s) are selected" — confirming Apply commits
  only the ticked item(s), not the whole machine.

## Objective
Verify the 'Select Items' dialog's 'Apply' button commits exactly the ticked items for backup
(not the entire physical server) and reflects that state correctly in the wizard's Source step.

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB\ft_psd`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-122670`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — UI assertion — `cd browser && python checks/check_njm_122670.py`: select machine,
  open Select Items, tick `ft_psd` folder only, Apply → **Expect:** footer/count and Source-step
  note both reflect exactly 1 item selected, and reopening the picker confirms it stuck.
  **VERIFIED PASS 2026-07-08.**
### Step 2 — Build (RPC confirmation the server-side job scope is item-level too) — R4c:
  `sourceVid=PM-3`, MappingBuilder select `C:/TestData_ForFLB/ft_psd` (`FOLDER`) only,
  `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-122670` → `saveJob`.
- **Expect:** `{OK, jobId}`; `objects[0].mappings` has exactly 1 entry.
### Step 3 — Run — R5 (`runType:ALL`).
### Step 4 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 5 — Verify — R7 · FLR `list` confirms only `ft_psd/` is in the recovery point.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if the UI check passes (verified) AND the built job's scope is limited to `ft_psd/` only,
reaching `lrState:OK` with that content confirmed in the recovery point.
