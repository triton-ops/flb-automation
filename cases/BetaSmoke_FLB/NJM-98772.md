# NJM-98772 — [FLB v1] Recovery from Tape - Verify Recovery from a Standalone Tape Drive

- **Source TC:** NJM-98772 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-98772, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ❌ BLOCKED (env) — this TC specifically requires a **standalone tape drive**
  (no robotic changer). The tape device added to nbr-84 2026-07-08 is `VLT_Tape`
  (`TAPE_LIBRARY-1`, `type:"TAPE_LIBRARY"`, 9 slots, robotic changer) — a **library**, not a
  standalone drive. This is a device-type mismatch, not merely "no tape hardware": a library's
  automated cartridge selection is a materially different code path from a standalone drive's
  manual-swap behavior, so this TC's actual assertion (standalone-drive recovery) still cannot be
  exercised here. See NJM-98773 (robotic library/VTL) for the TC in this batch that *does* match
  the available hardware.

## Objective
Verify recovery of an FLB backup from a standalone tape drive into a target disk repository.

## Fixtures used
- N/A — only a tape *library* (`VLT_Tape`) exists; no standalone tape drive.

## Preconditions
- [ ] **BLOCKED guard:** no standalone (non-robotic) tape drive present → BLOCKED(env). Do not
  substitute the tape library and call it equivalent — that would misrepresent the TC's actual
  scope (see CLAUDE.md rule 5, honest reporting).

## Steps
Not executed — blocked at precondition check.

## Cleanup
N/A — no job/artifact was created.

## Expected final verdict
BLOCKED(env) — only a robotic tape library exists in this lab, not a standalone drive as this TC
specifically requires.
