# NJM-98773 — [FLB v1] Recovery from Tape - Verify Recovery from a Robotic Tape Library/VTL

- **Source TC:** NJM-98773 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-98773, author=tri.ton, date=2026-07-08, product=FLB, status=GENERATED
- **Feasibility:** ⚠️ PARTIAL (upgraded from BLOCKED, 2026-07-08) — **`VLT_Tape`**
  (`TAPE_LIBRARY-1`), a robotic VTL (9 slots, 8 cartridges, changer), is exactly the device type
  this TC needs — plus media pool `test1` linked. Requires an existing FLB backup already written
  to it first (via NJM-70168's BC-to-tape pattern). Tape-target job shape and from-tape recovery
  flow both uncalibrated; needs live introspection first.

## Objective
Verify recovery of an FLB backup from a robotic tape library/VTL, including correct robotic-arm
tape loading behavior, into a target disk repository.

## Fixtures used
- Tape library: **VLT_Tape** (`TAPE_LIBRARY-1`); media pool: **test1** (id `1`)
- Depends on an existing FLB backup already written to tape (run NJM-70168 first if not)
- Repository (recovery target): **Onboard** (`BACKUP_REPOSITORY-2`)

## Preconditions
- [ ] An FLB backup already exists on `VLT_Tape` (run NJM-70168 first if not).
- [ ] **BLOCKED guard:** if no tape-based backup exists, BLOCKED(env).

## Steps
### Step 1 — Confirm a backup exists on `VLT_Tape` (Activities/tape inventory, or having just run
  NJM-70168).
### Step 2 — Introspect the tape-recovery RPC shape (uncalibrated, confirm live).
### Step 3 — Recover to Onboard; observe/confirm the robotic-arm cartridge load in the tape
  inventory/activity log during the operation.
### Step 4 — SHA-256 vs manifest to confirm content integrity survived the tape round-trip.

## Cleanup
- Remove any recovered/restored artifacts created solely for this test.

## Expected final verdict
PASS if the backup recovers from the robotic library intact, the robotic load is observable in
the activity log, and content matches source checksums. BLOCKED(env) if no tape-based backup
exists yet.
