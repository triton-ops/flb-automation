# NJM-67692 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on Windows Server 2025

- **Source TC:** NJM-67692 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67692, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `win2025-src` (PM-19) confirmed Windows Server 2025 Datacenter
  Evaluation. Newly discovered 2026-07-13 via `PhysicalDiscovery.create`.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a Windows Server 2025 source.

## Fixtures used
- Source: `win2025-src` **PM-19** (`Win_Server2025_15.245`) — Windows Server 2025 Datacenter Eval
  (note: this machine's target VID shifted from `PM-13` to `PM-19` between sessions — always
  re-check `PhysicalDiscovery.getDiscoveryItems` for the current VID before building a job)
- Test data: `C:\TestData_ForFLB\MixedTypes` (7 files), manifest `test-data/manifests/manifest-win2025-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-67692` (job id 90)

## Preconditions
- [x] PM-19 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-19 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-19`, mapping `C:/TestData_ForFLB/MixedTypes` (FOLDER), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-67692` → `saveJob`. **EXECUTED:** `{result:"OK", jobId:90}`.
### Step 2 — Run — R5 (`runType:ALL`). **CONFIRMED (rerun 2026-07-14):** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED.**
### Step 4 — Verify — R7 · savepoint + FLR session confirms scope.

## Cleanup
- **PASS** — verdict confirmed; job 90 cleanup (`JobManagement.remove [90, false]`) pending, to be
  done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK` confirmed; savepoint accessible.
