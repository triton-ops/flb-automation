# NJM-67700 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on Windows 11

- **Source TC:** NJM-67700 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67700, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `windows-src` (PM-3, `Window11`) is genuinely Windows 11 Pro.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a Windows 11 source.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`) — Windows 11 Pro, NTFS
- Test data: `C:\TestData_ForFLB\MixedTypes` (7 files: .pdf/.xml/.json/.docx/.sys/.jpg/.mp4), manifest `test-data/manifests/manifest-win11-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-67700` (job id 84)

## Preconditions
- [x] PM-3 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/MixedTypes` (FOLDER), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-67700` → `saveJob`. **EXECUTED:** `{result:"OK", jobId:84}`.
### Step 2 — Run — R5 (`runType:ALL`). **CONFIRMED (rerun 2026-07-14):** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED.**
### Step 4 — Verify — R7 · savepoint + FLR session confirms scope.

## Cleanup
- **PASS** — verdict confirmed; job 84 cleanup (`JobManagement.remove [84, false]`) pending, to be
  done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK` confirmed; savepoint accessible.
