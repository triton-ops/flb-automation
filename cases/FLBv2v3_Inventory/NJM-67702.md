# NJM-67702 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on Rocky Linux 9

- **Source TC:** NJM-67702 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67702, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `rocky9-src` (PM-22) confirmed Rocky Linux 9.5 (Blue Onyx). Machine
  appeared in NBR inventory pre-added (not by this automation session) and confirmed live 2026-07-14.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a Rocky Linux 9 source.

## Fixtures used
- Source: `rocky9-src` **PM-22** (`Rocky_Linux9`) — Rocky Linux 9.5
- Test data: `/TestData_ForFLB/MixedTypes` (7 files), manifest `test-data/manifests/manifest-rocky9-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4e** (scripted UI wizard — see recipes/file-backup-recipes.md); Job name:
  `AUTO_FLB_NJM-67702` (job id 102). A first attempt used R4c/`saveJob` (job id 91) but hit the
  RPC-only Linux FLR-browse defect (R4e note) — rebuilt via R4e and the old job removed.

## Preconditions
- [x] PM-22 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-22 — R0

## Steps
### Step 1 — Build — R4e: `browser/checks/build_flb_jobs_linux_batch.py`, machine `Rocky_Linux9`,
  drill `TestData_ForFLB` → tick `MixedTypes`, repo Onboard, name `AUTO_FLB_NJM-67702`.
  **EXECUTED 2026-07-14:** job id 102.
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-128`, spId 691) `isAccessible:true`. FLR
  session/UI browse confirmed: `TestData_ForFLB/MixedTypes` shows all 7 files with correct sizes
  (sample.docx 20.0KB, .jpg 8.0KB, .json, .mp4 64.0KB, .pdf, .sys 4.0KB, .xml) and timestamps —
  live-verified in the Director UI FLR wizard, 2026-07-14.

## Cleanup
- **PASS** — verdict confirmed; job 102 cleanup (`JobManagement.remove [102, false]`) pending, to
  be done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint accessible, FLR browse confirmed correct (files visible with
correct sizes) via the Director UI.
