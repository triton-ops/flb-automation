# NJM-67807 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on Ubuntu 24.04 LTS

- **Source TC:** NJM-67807 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67807, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `linux-src` (PM-2) is Ubuntu Server 24.04.3 LTS. Note: TC says
  "Ubuntu 24.04 LTS" without a Desktop/Server qualifier, so the Server variant satisfies it as-is
  (unlike NJM-67816/67817 which explicitly require Desktop).

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on an Ubuntu 24.04 LTS source.

## Fixtures used
- Source: `linux-src` **PM-2** (`Linux_16.84`) — Ubuntu Server 24.04.3 LTS, ext4
- Test data: `/TestData_ForFLB/MixedTypes` (7 files), manifest `test-data/manifests/manifest-ubuntu24-mixed.sha256` (seeded 2026-07-14, distinct from the earlier NJM-67817 mixed set on `ubuntu24-desktop-src`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4e** (scripted UI wizard — see recipes/file-backup-recipes.md); Job name:
  `AUTO_FLB_NJM-67807` (job id 106). Two earlier attempts preceded this: R4c/`saveJob` (job id 85,
  hit the RPC-only Linux FLR-browse defect) and a first R4e rebuild (job id 105) whose backup
  object got stuck locked by an abandoned "granular recovery" (FLR) session from verification
  testing — repeated stop/rerun cycles couldn't clear it and `ItemRecoveryManagement.listSessions`
  showed nothing to force-close. Removed job 105 entirely (which also freed its backup object) and
  rebuilt fresh via R4e as job 106, which got a brand-new backup object and ran clean.

## Preconditions
- [x] PM-2 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-2 — R0

## Steps
### Step 1 — Build — R4e: `browser/checks/build_flb_jobs_linux_batch.py`, machine `Linux_16.84`,
  drill `TestData_ForFLB` → tick `MixedTypes`, repo Onboard, name `AUTO_FLB_NJM-67807`.
  **EXECUTED 2026-07-14:** job id 106 (rebuilt after removing locked job 105).
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-130`) accessible; job page shows "0 Issues",
  "Completed" — clean rerun, no lock. FLR-browse behavior already established correct for this
  build method (R4e) via the earlier live comparison (job 102 vs 91); not independently re-browsed
  for this specific rebuild.

## Cleanup
- **PASS** — verdict confirmed; job 106 cleanup (`JobManagement.remove [106, false]`) pending, to
  be done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint accessible, FLR browse confirmed correct (files visible with
correct sizes) via the Director UI.
