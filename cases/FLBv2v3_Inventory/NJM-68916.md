# NJM-68916 — [FLB v1] FLB - Functional (Windows) - Verify Backup from FAT16/32, NTFS, and ReFS File Systems

- **Source TC:** NJM-68916 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68916, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ⚠️ RUNNABLE WITH SCOPE DEVIATION — the TC describes a single Windows machine
  with FAT16, FAT32, NTFS, and ReFS volumes all present. No single discovered machine has all
  four; the closest coverage found across the fleet is:
  - **FAT16** — `E:` on `win-fs3-src` (PM-9, `Win_Server2019_15.3`)
  - **FAT32** — `A:` on `win2022-src` (PM-11, `Win_Server2022_81.58`)
  - **NTFS** — `F:` on `win2022-src` (PM-11)
  - **ReFS** — `E:` on `win2022-src` (PM-11)
  This runbook builds **two separate single-source jobs** (one per machine) to cover all four
  filesystems, deviating from the TC's literal "1 machine" framing — noted here per CLAUDE.md
  honest-reporting rule, not hidden. (An earlier attempt combined both machines into one
  2-object job (id 95) but that failed because it shared a source physical machine with other
  concurrently-running jobs — see the job-run serialization rule in recipes.md. Split into two
  single-source jobs instead, which is also cleaner for the sequential-run constraint.)

## Objective
Verify FLB backup of items selected from FAT16, FAT32, NTFS, and ReFS volumes.

## Fixtures used
- Source A: `win-fs3-src` **PM-9** — volume `E:` (FAT16, ~1.1GB)
- Source B: `win2022-src` **PM-11** — volumes `A:` (FAT32, 12.8GB), `F:` (NTFS, 10.7GB), `E:` (ReFS, 5.3GB)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**, 2 single-object jobs; Job names: `AUTO_FLB_NJM-68916a` (job id 96, PM-9/FAT16)
  and `AUTO_FLB_NJM-68916b` (job id 97, PM-11/FAT32+NTFS+ReFS)

## Preconditions
- [x] PM-9 and PM-11 present & OK — R1
- [x] Onboard repo online — R3
- [x] All 4 target volumes exist with usable drive letters (verified live via `Get-Volume`) — no
  test-file seeding was performed on these volumes (whatever content already exists there is what
  gets backed up; the TC does not require content verification, only that each filesystem type
  backs up successfully)

## Steps
### Step 1 — Build — R4c: job 96 = `{sourceVid:"PM-9", mappings:[{sourceIdentifier:"E:/",
  sourceIdentifierType:"FOLDER"}]}`; job 97 = `{sourceVid:"PM-11", mappings:[{sourceIdentifier:
  "A:/"...}, {sourceIdentifier:"F:/"...}, {sourceIdentifier:"E:/"...}]}`, both
  `targetStorageVid=BACKUP_REPOSITORY-2` → `saveJob`. **EXECUTED:** job ids 96 and 97.
### Step 2 — Run — R5 (`runType:ALL`), run sequentially with respect to other jobs sharing PM-9/
  PM-11 (per the shared-source serialization rule). **EXECUTED (rerun 2026-07-14):** both
  `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` for both jobs.
### Step 4 — Verify — R7 · savepoint per object accessible; FLR session per object confirms each
  filesystem's scope (FAT16 for job 96; FAT32/NTFS/ReFS for job 97).

## Cleanup
- **PASS** — verdict confirmed; jobs 96 and 97 cleanup (`JobManagement.remove [96, false]` /
  `[97, false]`) pending, to be done in the same batch as the other PASS jobs from this Test
  Execution.

## Expected final verdict
**PASS.** Both jobs `lrState:OK` (`lrVmOk:1`, `lrVmFailed:0`), savepoints accessible for all four
filesystem types. Scope deviation (2 single-source jobs instead of 1 combined machine) is a
documented adaptation, not a defect.
