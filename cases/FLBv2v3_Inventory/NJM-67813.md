# NJM-67813 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on AlmaLinux 9

- **Source TC:** NJM-67813 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67813, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `almalinux9-src` (PM-17) confirmed AlmaLinux 9.4 (Seafoam Ocelot).
  Machine required a `firewalld` fix (`9446/tcp`) during discovery — see `environment.md` and
  `recipes/file-backup-recipes.md` for the gotcha.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on an AlmaLinux 9 source.

## Fixtures used
- Source: `almalinux9-src` **PM-17** (`AlmaLinux9_16.48`) — AlmaLinux 9.4, xfs root fs
- Test data: `/TestData_ForFLB/MixedTypes` (7 files), manifest `test-data/manifests/manifest-almalinux9-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4e** (scripted UI wizard — see recipes/file-backup-recipes.md); Job name:
  `AUTO_FLB_NJM-67813` (job id 109). A first attempt used R4c/`saveJob` (job id 86) — migrated to
  R4e 2026-07-15 along with the other remaining R4c-built Linux jobs in this suite (67816, 67817,
  68933, 68934), consistent with the Linux sources rebuilt earlier (67702/67806/67807/67808/67809).

## Preconditions
- [x] PM-17 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-17 — R0

## Steps
### Step 1 — Build — R4e: `browser/checks/build_flb_jobs_linux_batch.py`, machine
  `AlmaLinux9_16.48`, drill `TestData_ForFLB` → tick `MixedTypes`, repo Onboard, name
  `AUTO_FLB_NJM-67813`. **EXECUTED 2026-07-15:** job id 109 (rebuilt from R4c job 86).
### Step 2 — Run — R5 (`runType:ALL`). **CONFIRMED:** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED.**
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-136`) accessible.

## Evidence
**R7.4(a) listing-screenshot-pair — direct A/B confirmation of the R4c→R4e fix.** Compared the
Edit → Source → Select Items view of this job (109, R4e) against the prior R4c-built job 86 on
the *same* mapping/machine, live on nbr-84, 2026-07-15:
- **Job 86 (R4c/`saveJob`):** every top-level Linux folder (`afs`, `boot`, `dev`, `etc`, `home`,
  `media`, `mnt`, `opt`, `root`, `run`, `srv`, `sys`, `TestData_ForFLB`, `tmp`, `usr`, `var`) shows
  the **"partial selected"** (indeterminate/minus) checkbox — including `TestData_ForFLB` itself
  and every unrelated sibling. This is a **broader** manifestation of the known R4c defect than
  previously documented (the earlier note described it as mount-crossing-specific; here it
  reproduces on a plain root-volume mapping with no mount crossing at all).
- **Job 109 (R4e/scripted UI wizard), identical mapping:** only `TestData_ForFLB` (the real
  ancestor) shows partial-selected; every sibling folder is a plain, fully-unchecked box — the
  **correct** rendering.
- Screenshot-to-disk automation for this pair was attempted via a new script
  (`browser/checks/capture_evidence_pair.py`) but needs further Playwright locator calibration
  against this specific dialog (timed out waiting for the hover-revealed edit icon) — the finding
  above was confirmed by direct visual inspection instead. Follow-up: finish calibrating that
  script so this comparison can be captured as an image automatically in future runs.

## Cleanup
- **PASS** — verdict confirmed; job 109 cleanup (`JobManagement.remove [109, false]`) pending, to
  be done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK` confirmed; savepoint accessible; R4e build confirmed to render correctly
in Select Items (no partial-selected defect), unlike the earlier R4c build of the same mapping.
