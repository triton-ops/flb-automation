# NJM-67701 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on Windows 10 Enterprise

- **Source TC:** NJM-67701 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67701, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ⚠️ RUNNABLE WITH SCOPE DEVIATION — WinRM (5985 AND 5986) remains actively
  refused with confirmed credentials (`Administrator`/`ipc@12Ipc`) on 10.10.17.109; this means
  WinRM is simply not enabled on the box (not a firewall-port issue like the earlier Linux
  cases), which needs local/console access to fix (`winrm quickconfig` / `Enable-PSRemoting`) —
  not something bootstrappable via remoting alone. However, `PM-25` (`Window_10`) is already
  `state:OK` in NBR's own `PhysicalDiscovery` (NBR deploys its physical agent independently of
  WinRM), so the backup job itself is buildable/runnable right now. Per user direction, proceeded
  using the `TestData_ForFLB` fileset **already present on the machine** (browsed via
  `PhysicalDiscovery.getFolders`, not seeded by this session) rather than waiting for WinRM —
  same precedent as NJM-68916's documented scope deviation. OS version was not independently
  re-confirmed via remoting (WinRM still blocked); NBR's own discovery identifies it as `Window_10`.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a Windows 10 Enterprise source.

## Fixtures used
- Source: `win10-src` **PM-25** (`Window_10`)
- Test data: pre-existing `C:\TestData_ForFLB` (the standard `ft_*`/`Subfolder_200Folders`/
  `FolderEmpty_test4-5` fileset, browsed via `PhysicalDiscovery.getFolders` — not independently
  checksummed since remoting access is unavailable)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** (`saveJob` — Windows sources are unaffected by the Linux-only RPC FLR-browse
  defect, see recipes/file-backup-recipes.md); Job name: `AUTO_FLB_NJM-67701` (job id 108)

## Preconditions
- [x] PM-25 present & OK — R1
- [x] Onboard repo online — R3
- [x] Test data present on PM-25 (pre-existing `C:\TestData_ForFLB`, confirmed via
  `PhysicalDiscovery.getFolders`) — R0 (no seeding performed; WinRM unavailable)

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-25`, mapping `C:/TestData_ForFLB` (FOLDER),
  `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-67701` → `saveJob`.
  **EXECUTED 2026-07-14:** `{result:"OK", jobId:108}`.
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-132`) created. FLR browse not independently
  re-verified in this pass (Windows RPC-built jobs have no known FLR-browse defect — confirmed
  clean for every other Windows TC in this Test Execution built the same way).

## Cleanup
- **PASS** — verdict confirmed; job 108 cleanup (`JobManagement.remove [108, false]`) pending, to
  be done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint created. Scope deviation (test data not independently seeded/
checksummed due to WinRM being unavailable) is a documented adaptation, not a defect — flagged
per CLAUDE.md honest-reporting rule.
