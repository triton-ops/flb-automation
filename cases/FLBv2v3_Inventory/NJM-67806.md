# NJM-67806 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on Debian 12

- **Source TC:** NJM-67806 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67806, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `debian12-src` (PM-23) confirmed Debian GNU/Linux 12 (bookworm).
  Machine appeared in NBR inventory pre-added (not by this automation session) and confirmed live 2026-07-14.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a Debian 12 source.

## Fixtures used
- Source: `debian12-src` **PM-23** (`Debian_12`) — Debian GNU/Linux 12 (bookworm)
- Test data: `/TestData_ForFLB/MixedTypes` (7 files), manifest `test-data/manifests/manifest-debian12-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4e** (scripted UI wizard — see recipes/file-backup-recipes.md); Job name:
  `AUTO_FLB_NJM-67806` (job id 103). A first attempt used R4c/`saveJob` (job id 92) but hit the
  RPC-only Linux FLR-browse defect (R4e note) — rebuilt via R4e and the old job removed.

## Preconditions
- [x] PM-23 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-23 — R0

## Steps
### Step 1 — Build — R4e: `browser/checks/build_flb_jobs_linux_batch.py`, machine `Debian_12`,
  drill `TestData_ForFLB` → tick `MixedTypes`, repo Onboard, name `AUTO_FLB_NJM-67806`.
  **EXECUTED 2026-07-14:** job id 103.
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-126`) `isAccessible:true`. FLR session/UI
  browse confirmed: `TestData_ForFLB/MixedTypes` shows all 7 files with correct sizes and
  timestamps — live-verified in the Director UI FLR wizard, 2026-07-14.

## Cleanup
- **PASS** — verdict confirmed; job 103 cleanup (`JobManagement.remove [103, false]`) pending, to
  be done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint accessible, FLR browse confirmed correct (files visible with
correct sizes) via the Director UI.
