# NJM-67809 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on SLES 15 SP6

- **Source TC:** NJM-67809 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67809, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `sles15-src` (PM-20) confirmed SUSE Linux Enterprise Server 15 SP6.
  Machine appeared in NBR inventory pre-added (not by this automation session) and confirmed live 2026-07-14.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a SLES 15 SP6 source.

## Fixtures used
- Source: `sles15-src` **PM-20** (`SLE_SUSE_15.205`) — SLES 15 SP6
- Test data: `/TestData_ForFLB/MixedTypes` (7 files), manifest `test-data/manifests/manifest-sles15-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4e** (scripted UI wizard — see recipes/file-backup-recipes.md); Job name:
  `AUTO_FLB_NJM-67809` (job id 104). A first attempt used R4c/`saveJob` (job id 93) but hit the
  RPC-only Linux FLR-browse defect (R4e note) — rebuilt via R4e and the old job removed.

## Preconditions
- [x] PM-20 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-20 — R0

## Steps
### Step 1 — Build — R4e: `browser/checks/build_flb_jobs_linux_batch.py`, machine
  `SLE_SUSE_15.205`, drill `TestData_ForFLB` → tick `MixedTypes`, repo Onboard, name
  `AUTO_FLB_NJM-67809`. **EXECUTED 2026-07-14:** job id 104.
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-127`) `isAccessible:true`. FLR session/UI
  browse confirmed: `TestData_ForFLB/MixedTypes` shows all 7 files with correct sizes and
  timestamps — live-verified in the Director UI FLR wizard, 2026-07-14.

## Cleanup
- **PASS** — verdict confirmed; job 104 cleanup (`JobManagement.remove [104, false]`) pending, to
  be done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint accessible, FLR browse confirmed correct (files visible with
correct sizes) via the Director UI.
