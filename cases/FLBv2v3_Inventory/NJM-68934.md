# NJM-68934 — [FLB v1] FLB - Functional (Linux) - Verify Backup of XFS File System

- **Source TC:** NJM-68934 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68934, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `ubuntu22-xfs-vol` is a dedicated second disk (`/dev/sdb1`, 16GB)
  on `ubuntu22-desktop-src` (PM-14), user-added and formatted **XFS** by this automation
  2026-07-13 (see `environment.md` / `test-data.md`).
- **History:** first executed 2026-07-13 as job 60 → `lrState:OK`, savepoint accessible — the
  user then deleted job 60 manually during a live debugging session (unrelated to this TC; see
  the "partial selected" UI-gotcha investigation in `recipes/file-backup-recipes.md`). Re-built
  as job 94 on 2026-07-14 to restore evidence.

## Objective
Verify FLB backup of data residing on an XFS-formatted volume.

## Fixtures used
- Source: `ubuntu22-xfs-vol` **PM-14** (`Ubuntu2204Desktop_16.98`), mount `/mnt/xfs_testdata`
- Test data: `/mnt/xfs_testdata/TestData_XFS` (5 files, ~3.1MB), manifest `test-data/manifests/manifest-ubuntu22-xfs.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68934` (job id 94, rebuilt 2026-07-14)

## Preconditions
- [x] PM-14 present & OK — R1
- [x] Onboard repo online — R3
- [x] XFS volume formatted & seeded — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-14`, mapping `/mnt/xfs_testdata/TestData_XFS` (FOLDER), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68934` → `saveJob`. **EXECUTED 2026-07-14:** `{result:"OK", jobId:94}`.
### Step 2 — Run — R5 (`runType:ALL`). **CONFIRMED (rerun 2026-07-14):** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED.**
### Step 4 — Verify — R7 · savepoint + FLR session confirms scope.

> Known cosmetic gotcha: re-opening this job's Select Items dialog in the Director UI will show
> every top-level Linux folder as "partial selected" (not just `mnt`) because the mapping crosses
> a mount-point boundary and was built via RPC — see `recipes/file-backup-recipes.md`. Does not
> affect the backup itself.

## Cleanup
- **PASS** — verdict confirmed; job 94 cleanup (`JobManagement.remove [94, false]`) pending, to be
  done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK` confirmed; savepoint accessible.
