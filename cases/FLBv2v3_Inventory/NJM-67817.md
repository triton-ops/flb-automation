# NJM-67817 — [FLB v1] FLB - Functional (Linux) - Verify Backup from Ubuntu 24.04 LTS Desktop, mixed file types

- **Source TC:** NJM-67817 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67817, author=tri.ton, date=2026-07-13, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `ubuntu24-desktop-src` (PM-15) confirmed genuine Ubuntu 24.04.4 LTS
  **Desktop** (`ubuntu-desktop-minimal` package present).

## Objective
Verify FLB backup of mixed-type files (.pdf/.xml/.json/.docx/.sys/.jpg/.mp4) on Ubuntu 24.04 LTS
Desktop, then FLR-recover and compare with source.

## Fixtures used
- Source: `ubuntu24-desktop-src` **PM-15** (`Ubuntu2404Desktop_16.119`) — Ubuntu 24.04 LTS Desktop, ext4
- Test data: `/TestData_ForFLB/MixedTypes` (7 files), manifest `test-data/manifests/manifest-ubuntu24-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-67817` (job id 62)

## Preconditions
- [x] PM-15 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-15 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-15`, mapping `/TestData_ForFLB/MixedTypes` (FOLDER), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-67817` → `saveJob`. **EXECUTED 2026-07-13:** `{result:"OK", jobId:62}`.
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-86`, spId 634) `isAccessible:true`. FLR session mount confirmed correct top-level scope. Full byte-level checksum export vs manifest **not performed** (no CIFS/NFS export credential available this session).
### Step 5 — Evidence — Screenshot `results/screenshots/NJM-67817__20260713/01_jobs_view.png`.

## Cleanup
- **PASS** — verdict confirmed; job 62 cleanup (`JobManagement.remove [62, false]`) pending, to be
  done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint accessible, FLR session confirmed correct scope. Full checksum
compare deferred (missing export credential).
