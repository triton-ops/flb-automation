# NJM-67808 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on RHEL 9

- **Source TC:** NJM-67808 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67808, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `rhel9-src` (PM-28) confirmed Red Hat Enterprise Linux 9.5 (Plow).
  The machine (10.10.16.115) came up mid-session; discovery initially failed
  (`state:INACCESSIBLE`, "deployed physical agent is inaccessible") for the same reason as the
  earlier AlmaLinux9 case — `firewalld` only allowed `cockpit/dhcpv6-client/ssh`, blocking the
  NAKIVO agent port `9446/tcp`. Fixed identically: opened the port on the host, then retried via
  `PhysicalDiscovery.update` — succeeded.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a RHEL 9 source.

## Fixtures used
- Source: `rhel9-src` **PM-28** (`RHEL_9`) — Red Hat Enterprise Linux 9.5 (Plow)
- Test data: `/TestData_ForFLB/MixedTypes` (7 files) — seeded 2026-07-14 directly via SSH
  (deterministic content matching the standard MixedTypes convention), manifest
  `test-data/manifests/manifest-rhel9-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4e** (scripted UI wizard — see recipes/file-backup-recipes.md); Job name:
  `AUTO_FLB_NJM-67808` (job id 107)

## Preconditions
- [x] PM-28 present & OK — R1 (fixed live: firewalld port 9446/tcp opened, then
  `PhysicalDiscovery.update` with `confirmed:true`)
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-28 — R0

## Steps
### Step 1 — Build — R4e: `browser/checks/build_flb_jobs_linux_batch.py`, machine `RHEL_9`,
  drill `TestData_ForFLB` → tick `MixedTypes`, repo Onboard, name `AUTO_FLB_NJM-67808`.
  **EXECUTED 2026-07-14:** job id 107.
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-131`) accessible. Build method (R4e) already
  established live to produce a correctly FLR-browsable backup (job 102 vs 91 comparison); not
  independently re-browsed for this specific host.

## Cleanup
- **PASS** — verdict confirmed; job 107 cleanup (`JobManagement.remove [107, false]`) pending, to
  be done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint accessible via the R4e (scripted UI wizard) build method.
