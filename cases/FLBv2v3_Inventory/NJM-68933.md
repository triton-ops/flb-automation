# NJM-68933 — [FLB v1] FLB - Functional (Linux) - Verify Backup of EXT4 File System

- **Source TC:** NJM-68933 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68933, author=tri.ton, date=2026-07-13, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `linux-src` (PM-2) root filesystem is **ext4**.

## Objective
Verify FLB backup of data residing on an EXT4-formatted volume.

> Scope note: the TC's Data column says "select the EXT4-formatted volume" — this run backs up
> the `/TestData_ForFLB` folder, which resides on the EXT4 root volume (there is no separate
> EXT4-only volume on this machine), satisfying the TC's intent of exercising FLB against ext4 data.

## Fixtures used
- Source: `linux-src` **PM-2** (`Linux_16.84`) — ext4 root fs
- Test data: existing `/TestData_ForFLB` fileset (341 files, ~242MB), manifest `test-data/manifests/manifest-linux.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68933` (job id 59)

## Preconditions
- [x] PM-2 present & OK — R1
- [x] Onboard repo online — R3
- [x] Test data already seeded (existing fileset) — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-2`, mapping `/TestData_ForFLB` (FOLDER), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68933` → `saveJob`. **EXECUTED 2026-07-13:** `{result:"OK", jobId:59}`.
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-83`, spId 631, `consumed:150,564,173` bytes) `isAccessible:true`. FLR session mount confirmed correct top-level scope (`TestData_ForFLB` node under root).
### Step 5 — Evidence — Screenshot `results/screenshots/NJM-68933__20260713/01_jobs_view.png`.

## Cleanup
- **PASS** — verdict confirmed; job 59 cleanup (`JobManagement.remove [59, false]`) pending, to be
  done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint accessible, FLR session confirmed correct scope on the ext4 volume.
