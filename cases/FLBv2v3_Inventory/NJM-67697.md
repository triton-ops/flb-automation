# NJM-67697 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on Windows Server 2016

- **Source TC:** NJM-67697 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67697, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `win2016-src` (PM-12) confirmed Windows Server 2016 Datacenter
  Evaluation. Newly discovered 2026-07-13 via `PhysicalDiscovery.create`.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a Windows Server 2016 source.

## Fixtures used
- Source: `win2016-src` **PM-12** (`Win_Server2016_15.19`) — Windows Server 2016 Datacenter Eval
- Test data: `C:\TestData_ForFLB\MixedTypes` (7 files), manifest `test-data/manifests/manifest-win2016-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-67697` (job id 87)

## Preconditions
- [x] PM-12 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-12 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-12`, mapping `C:/TestData_ForFLB/MixedTypes` (FOLDER), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-67697` → `saveJob`. **EXECUTED:** `{result:"OK", jobId:87}`.
### Step 2 — Run — R5 (`runType:ALL`). **CONFIRMED (rerun 2026-07-14):** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED.**
### Step 4 — Verify — R7 · savepoint + FLR session confirms scope.

## Cleanup
- **PASS** — verdict confirmed; job 87 cleanup (`JobManagement.remove [87, false]`) pending, to be
  done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK` confirmed; savepoint accessible.
