# NJM-67694 — [FLB v1] FLB - OS Support - Verify End-to-End Workflow on Windows Server 2019

- **Source TC:** NJM-67694 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67694, author=tri.ton, date=2026-07-14, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `win-fs3-src` (PM-9) confirmed Windows Server 2019 Datacenter.
  **Scope note:** this machine's primary role in the framework is the NFS/CIFS share host + FLR
  export target (see `environment.md`) — it is being reused here as an FLB source in its
  secondary role; the share-host role is unaffected.

## Objective
Verify FLB end-to-end workflow (create → run → FLR recover → compare) on a Windows Server 2019 source.

## Fixtures used
- Source: `win-fs3-src` **PM-9** (`Win_Server2019_15.3`) — Windows Server 2019 Datacenter
- Test data: `C:\TestData_ForFLB\MixedTypes` (7 files), manifest `test-data/manifests/manifest-win2019-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-67694` (job id 88)

## Preconditions
- [x] PM-9 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-9 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-9`, mapping `C:/TestData_ForFLB/MixedTypes` (FOLDER), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-67694` → `saveJob`. **EXECUTED:** `{result:"OK", jobId:88}`.
### Step 2 — Run — R5 (`runType:ALL`). **CONFIRMED:** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`. **CONFIRMED.**
### Step 4 — Verify — R7 · savepoint + FLR session confirms scope.

## Cleanup
- PASS → R9 (`remove [88, false]`).

## Expected final verdict
PASS — `lrState:OK` confirmed; savepoint accessible.
