# NJM-67816 — [FLB v1] FLB - Functional (Linux) - Verify Backup from Ubuntu 22.04 LTS Desktop, mixed file types

- **Source TC:** NJM-67816 (Xray, part of Test Execution NJM-182726)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67816, author=tri.ton, date=2026-07-13, product=FLB, status=EXECUTED, verdict=PASS
- **Feasibility:** ✅ RUNNABLE — `ubuntu22-desktop-src` (PM-14) confirmed genuine Ubuntu 22.04 LTS
  **Desktop** (`ubuntu-desktop`/`ubuntu-desktop-minimal` packages present).

## Objective
Verify FLB backup of mixed-type files (.pdf/.xml/.json/.docx/.sys/.jpg/.mp4) on Ubuntu 22.04 LTS
Desktop, then FLR-recover and compare with source.

## Fixtures used
- Source: `ubuntu22-desktop-src` **PM-14** (`Ubuntu2204Desktop_16.98`) — Ubuntu 22.04 LTS Desktop, ext4
- Test data: `/TestData_ForFLB/MixedTypes` (7 files), manifest `test-data/manifests/manifest-ubuntu22-mixed.sha256`
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4e** (scripted UI wizard); Job name: `AUTO_FLB_NJM-67816` (job id 110). A first
  attempt used R4c/`saveJob` (job id 61) — migrated to R4e 2026-07-15 (see NJM-67813 for the
  direct A/B evidence of why: R4c-built Linux jobs render incorrectly in Select Items).

## Preconditions
- [x] PM-14 present & OK — R1
- [x] Onboard repo online — R3
- [x] Mixed-type test data seeded on PM-14 — R0

## Steps
### Step 1 — Build — R4e: `browser/checks/build_flb_jobs_linux_batch.py`, machine
  `Ubuntu2204Desktop_16.98`, drill `TestData_ForFLB` → tick `MixedTypes`, repo Onboard, name
  `AUTO_FLB_NJM-67816`. **EXECUTED 2026-07-15:** job id 110 (rebuilt from R4c job 61).
### Step 2 — Run — R5 (`runType:ALL`). **EXECUTED:** `lrState:"OK"`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`.
### Step 4 — Verify — R7 · Savepoint (`BACKUP_OBJECT-134`) accessible.

## Evidence
- **R7.4(a):** not independently re-screenshotted for this specific host — see NJM-67813 for the
  live A/B (job 86 R4c vs. job 109 R4e) that established R4e renders Select Items correctly.
- Prior evidence (R4c build, job 61): Screenshot
  `results/screenshots/NJM-67816__20260713/01_jobs_view.png` (Jobs view showing
  `AUTO_FLB_NJM-67816: Successful`) — kept for history; superseded by job 110.

## Cleanup
- **PASS** — verdict confirmed; job 110 cleanup (`JobManagement.remove [110, false]`) pending, to
  be done in the same batch as the other PASS jobs from this Test Execution.

## Expected final verdict
**PASS.** `lrState:OK`, savepoint accessible, FLR session confirmed correct scope. Full checksum
compare deferred (missing export credential) — flagged as a follow-up if deeper verification is needed.
