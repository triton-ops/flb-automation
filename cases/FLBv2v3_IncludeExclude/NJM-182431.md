# NJM-182431 — [FLB v3] FLB Job Wizard - Inclusion/Exclusion UI - Step 2/3 layout and controls (FLB-11/FLB-12)

- **Source TC:** NJM-182431 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard UI
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-182431, author=tri.ton, date=2026-07-15, product=FLB, status=PENDING

## Objective
Verify the Step 2 - Inclusion / Step 3 - Exclusion wizard UI shows the expected controls and that
rules can be added/removed. **UI-behavior check — no backup job needs to actually run.**

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`) — any reachable machine, no specific test data needed
- No repository/run required — this TC only exercises wizard navigation

## Preconditions
- [ ] PM-3 present & OK — R1

## Steps
### Step 1 — Open the FLB wizard, advance to Step 2 - Inclusion (browser: `FlbWizardPage`, R4e
  wizard flow). **Note:** the current calibrated POM only has a single "Include items"/"Exclude
  items" checkbox + free-text textarea (see `InclusionExclusionLocators` in
  `recipes/file-backup-recipes.md`) — there is no separate "Extension / File name / Path"
  rule-type selector control as the TC description implies. This is a **spec-vs-actual-UI
  discrepancy** to confirm live, not assume from the recipe notes alone.
### Step 2 — Inspect Step 2 - Inclusion controls; screenshot.
### Step 3 — Inspect Step 3 - Exclusion controls; screenshot.
### Step 4 — Add then remove a rule on each step; confirm the textarea reflects the change.

## Cleanup
- No job created — nothing to clean up (cancel the wizard without saving).

## Expected final verdict
PASS if both steps render usable add/remove controls and changes are reflected live. If the
rule-type-selector control described in the TC (Extension/File name/Path as separate UI elements)
does not exist in the current build, report as **FAIL** (or note a spec deviation) rather than
silently passing — confirm the actual UI live before writing the verdict.
