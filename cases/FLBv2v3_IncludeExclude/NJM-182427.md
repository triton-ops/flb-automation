# NJM-182427 — [FLB v3] FLB Job Wizard - Step 3 Exclusion - Exclude all .pdf files [UC3] (FLB-12)

- **Source TC:** NJM-182427 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 3 Exclusion
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-182427, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify that an Extension exclusion rule (`*.pdf`) excludes all `.pdf` files while backing up
everything else.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\MixedTypes\` — `sample.docx`, `sample.pdf`,
  `sample.jpg` (shared fixture with NJM-182424)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Exclusion (`enabledSourceItemsExclusion=true`, `sourceItemsExclusion="*.pdf"`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `MixedTypes` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/IncludeExclude/MixedTypes`
  (FOLDER), exclusion rule `*.pdf`, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-182427` → `saveJob`. **EXECUTED 2026-07-15:** job id 123.
### Step 2 — Run — R5. First run launched concurrently alongside job 124 (same PM-3 source) and
  both **stalled indefinitely** (`crState:RUNNING`, `crProgress` frozen ~6%, no failure event) —
  **root cause: NBR locks a physical-machine source for one job at a time; concurrent jobs
  against the same source VID contend/deadlock rather than queueing cleanly.** Fixed by stopping
  both (`JobManagement.stop`) and re-running **sequentially, one job per source at a time**.
  Re-run: `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-148`, `SAVEPOINT-766`).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\MixedTypes\` →
  **CONFIRMED** `sample.docx`/`sample.jpg` present; `sample.pdf` absent.

## Cleanup
- **PASS** — job 123 removed (`JobManagement.remove [123, false]`).

## Expected final verdict
**PASS.** `.pdf` excluded, all non-`.pdf` files backed up correctly.
