# NJM-185023 — [FLB v3] FLB Job Wizard - Step 2/3 - Inclusion/Exclusion applied consistently across initial + 3 incremental runs (FLB-11/FLB-12)

- **Source TC:** NJM-185023 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2+3, incremental behavior
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185023, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify Inclusion/Exclusion filters remain consistently applied across an initial full backup and
3 subsequent incremental runs, even as source data changes between runs.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\IncrementalConsistency\` — starts with
  `keep1.docx`, `exclude1.tmp` (to be seeded); modified between runs per Step 3/4/5 below
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Inclusion (`*.docx`) + Exclusion (`*.tmp`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `IncrementalConsistency` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping
  `C:/TestData_ForFLB/IncludeExclude/IncrementalConsistency` (FOLDER), inclusion `*.docx`,
  exclusion `*.tmp`, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-185023` →
  `saveJob`. **EXECUTED 2026-07-15:** job id 133.
### Step 2 — Run full backup — R5/R6. **EXECUTED:** `lrState:OK`; **savepoint 1** (`SAVEPOINT-774`,
  `type:FULL`, `consumed:2`) verified via FLR browse — only `keep1.docx` present.
### Step 3 — Add `keep2.docx` + `exclude2.tmp` (via `winrm_run`); run incremental #1 — R5/R6.
  **EXECUTED:** `lrState:OK`; **savepoint 2** (`SAVEPOINT-775`, `type:INCREMENTAL`, `consumed:2`)
  verified via FLR browse — `keep1.docx`+`keep2.docx` present, no `.tmp` files.
### Step 4 — Add `keep3.docx` + `exclude3.tmp`; run incremental #2 — R5/R6. **EXECUTED:**
  `lrState:OK`; **savepoint 3** (`SAVEPOINT-776`, `consumed:2`) verified — `keep1`–`keep3.docx`
  present, no `.tmp` files.
### Step 5 — Add `keep4.docx` + `exclude4.tmp`; run incremental #3 — R5/R6. **EXECUTED:**
  `lrState:OK`; **savepoint 4** (`SAVEPOINT-777`, `consumed:2`).
### Step 6 — Verify — R7 · FLR browse the final savepoint (777, synthesizing the full
  incremental chain) → **CONFIRMED** all 4 `keep*.docx` (1–4) present; all 4 `exclude*.tmp`
  (1–4) absent. Each of the 4 savepoints individually browsed and confirmed correct at its own
  point in the chain (see Steps 2–5) — the filter never let an excluded file slip through at
  any stage.

## Cleanup
- **PASS** — job 133 removed (`JobManagement.remove [133, false]`).

## Expected final verdict
**PASS.** The Inclusion/Exclusion filter is consistently applied across the initial full backup
and all 3 subsequent incremental runs — no excluded content ever appeared in any of the 4
recovery points, confirmed both per-run and in the final synthesized view.
