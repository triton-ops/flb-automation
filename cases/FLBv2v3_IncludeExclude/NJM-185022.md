# NJM-185022 — [FLB v3] FLB Job Wizard - Inclusion + Exclusion - Overrule Example C (same parameter in both -> excluded) (FLB-11/FLB-12)

- **Source TC:** NJM-185022 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2+3 combined
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185022, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify: the same parameter set in BOTH Inclusion and Exclusion (`data.bin`) → the item is
excluded (exclusion wins on a direct conflict).

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\BinOverrule\` — `data.bin`, `other.txt`
  (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Inclusion (`data.bin`) + Exclusion (`data.bin`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `BinOverrule` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/IncludeExclude/BinOverrule`
  (FOLDER), inclusion `data.bin`, exclusion `data.bin`, `targetStorageVid=BACKUP_REPOSITORY-2`,
  name `AUTO_FLB_NJM-185022` → `saveJob`. **EXECUTED 2026-07-15:** job id 131.
### Step 2 — Run — R5. **EXECUTED:** `lrState:OK`, `lrVmOk:1` (job succeeds despite the
  resulting item set being empty — same as NJM-185021).
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-154`, `SAVEPOINT-772`,
  `consumed:0` bytes).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\BinOverrule\` →
  **CONFIRMED** empty listing (0 items) — since `data.bin` was the only inclusion match and it
  was also excluded, the recovery point is entirely empty (as anticipated in the runbook design).

## Cleanup
- **PASS** — job 131 removed (`JobManagement.remove [131, false]`).

## Expected final verdict
**PASS.** `data.bin` excluded on direct conflict (exclusion wins); recovery point is empty
since it was the only file matching the inclusion rule.
