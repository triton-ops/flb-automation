# NJM-182424 — [FLB v3] FLB Job Wizard - Step 2 Inclusion - Include files by extension (FLB-11)

- **Source TC:** NJM-182424 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2 Inclusion
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-182424, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify that adding an Extension inclusion rule (`*.docx`) on Step 2 - Inclusion backs up only
files matching that extension, skipping everything else.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\MixedTypes\` — `sample.docx`, `sample.pdf`,
  `sample.jpg` (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Inclusion fields (`options.enabledSourceItemsInclusion=true`,
  `options.sourceItemsInclusion="*.docx"`) — see recipes/file-backup-recipes.md R4-Filter

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `MixedTypes` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/IncludeExclude/MixedTypes`
  (FOLDER), `options.enabledSourceItemsInclusion=true`, `options.sourceItemsInclusion="*.docx"`,
  `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-182424` → `saveJob`.
  **EXECUTED 2026-07-15:** job id 114. First `saveJob` call must wrap the JobDto as
  `{"origin":"NONE","job":<JobDto>}` — passing the raw JobDto directly throws a server-side NPE
  (`dto.getId()` on a null `dto`).
### Step 2 — Run — R5 (`runType:ALL`).
  **First attempt FAILED** (`lrState:FAILED`, ~8s): Director event log showed
  `BackupEnforcePreconditions: the infrastructure has changed` →
  `The "C:/TestData_ForFLB/IncludeExclude/MixedTypes" folder cannot be found` — even though the
  folder demonstrably existed on PM-3 (confirmed via WinRM `Get-ChildItem`). Root cause: PM-3's
  `PhysicalDiscovery` item was discovered before this Test Execution's fixtures were seeded onto
  it, and its cached folder-tree snapshot was stale. **Fix:** call
  `PhysicalDiscovery.refreshAll []` (async), wait ~20s, confirm via
  `PhysicalDiscovery.getFolders ["PM-3","C:/TestData_ForFLB/IncludeExclude"]` that the new folders
  are now visible, then re-run. Second run: **EXECUTED**, `lrState:OK`, `lrVmOk:1`.
  ⚠️ **This applies to every TC in this Test Execution** — `refreshAll` was run once (covers all
  discovery items, both PM-3 and PM-2) and should not need repeating for the remaining 20 TCs
  unless the appliance re-discovers those items again.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK`, `lrVmOk:1` (job 114, `BACKUP_OBJECT-138`,
  `SAVEPOINT-756`).
### Step 4 — Verify — R7 · FLR `createSession {hvType:"PHYSICAL", type:"BACKUP_OBJECT", id:138,
  spId:756}` → `list` on `C:\TestData_ForFLB\IncludeExclude\MixedTypes\` → **CONFIRMED** only
  `sample.docx` (100 bytes) present; `sample.pdf`/`sample.jpg` correctly excluded by the
  `*.docx` inclusion rule.

## Cleanup
- **PASS** — job 114 removed (`JobManagement.remove [114, false]`).

## Expected final verdict
**PASS.** Recovery point contains `sample.docx` only (`.pdf`/`.jpg` skipped) — Include filter by
extension confirmed working via raw `saveJob` (`options.enabledSourceItemsInclusion` +
`sourceItemsInclusion`).
