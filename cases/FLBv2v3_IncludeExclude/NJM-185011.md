# NJM-185011 — [FLB v3] FLB Job Wizard - Step 2 Inclusion - Include files by name wildcard '*Report*' (FLB-11)

- **Source TC:** NJM-185011 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2 Inclusion
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185011, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify an inclusion rule using `*Report*` (wildcard both sides) backs up only names containing
"Report", regardless of position.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\ByName\` — `Q1Report.docx`, `Report_final.pdf`,
  `budget.xlsx`, `notes.txt` (shared fixture with NJM-182425)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Inclusion (`*Report*`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `ByName` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/IncludeExclude/ByName`
  (FOLDER), inclusion rule `*Report*`, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-185011` → `saveJob`. **EXECUTED 2026-07-15:** job id 117.
### Step 2 — Run — R5. **EXECUTED:** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-140`, `SAVEPOINT-758`).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\ByName\` →
  **CONFIRMED** both `Q1Report.docx` and `Report_final.pdf` present; `budget.xlsx`/`notes.txt`
  absent — `*Report*` (wildcard both sides) correctly matches "Report" anywhere in the name,
  unlike the prefix-only `report*` rule in NJM-182425.

## Cleanup
- **PASS** — job 117 removed (`JobManagement.remove [117, false]`).

## Expected final verdict
**PASS.** Both "Report"-containing names present regardless of position; non-matching files
absent.
