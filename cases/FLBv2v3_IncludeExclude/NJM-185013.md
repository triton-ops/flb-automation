# NJM-185013 — [FLB v3] FLB Job Wizard - Step 2 Inclusion - Wildcard pattern table validation (*.*, abc*, abc?, *share, Na??vo.log) (FLB-11)

- **Source TC:** NJM-185013 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2 Inclusion
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185013, author=tri.ton, date=2026-07-15, product=FLB, status=PENDING

## Objective
Verify the Step 2 - Inclusion parameter box accepts all five spec wildcard pattern shapes as
valid: `*.*`, `abc*`, `abc?`, `*share`, `Na??vo.log`. **UI-behavior/validation check — no backup
job needs to actually run**, unless validation itself requires a completed job to confirm.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`) — any reachable machine
- No specific test data required — this is textarea input validation only

## Preconditions
- [ ] PM-3 present & OK — R1

## Steps
### Step 1 — Open the FLB wizard, advance to Step 2 - Inclusion, enable "Include items".
### Step 2 — Enter each pattern on its own line: `*.*`, `abc*`, `abc?`, `*share`, `Na??vo.log`.
### Step 3 — Confirm the textarea does NOT turn red (no "Invalid parameters" message) and
  Next/Apply remains enabled.
### Step 4 — Screenshot the populated textarea as evidence.

## Cleanup
- No job created — cancel the wizard without saving.

## Expected final verdict
PASS if all five patterns are accepted without validation error.
