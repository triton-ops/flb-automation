# NJM-185012 — [FLB v3] FLB Job Wizard - Step 3 Exclusion - Exclude by extension wildcard '*.csv' (FLB-12)

- **Source TC:** NJM-185012 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 3 Exclusion
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185012, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify an exclusion rule `*.csv` excludes all `.csv` files, keeping non-`.csv` files.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\CsvTest\` — `data.csv`, `report.csv`, `a.docx`,
  `b.pdf` (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Exclusion (`*.csv`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `CsvTest` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/IncludeExclude/CsvTest`
  (FOLDER), exclusion rule `*.csv`, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-185012` → `saveJob`. **EXECUTED 2026-07-15:** job id 125.
### Step 2 — Run — R5. **EXECUTED:** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-146`, `SAVEPOINT-764`).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\CsvTest\` →
  **CONFIRMED** `a.docx`/`b.pdf` present; `data.csv`/`report.csv` absent.

## Cleanup
- **PASS** — job 125 removed (`JobManagement.remove [125, false]`).

## Expected final verdict
**PASS.** All `.csv` files excluded; non-`.csv` files backed up correctly.
