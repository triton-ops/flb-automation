# NJM-185019 — [FLB v3] FLB Job Wizard - Step 3 Exclusion - Foreign-language folder names (Arabic, Chinese, Korean, Russian) (FLB-12)

- **Source TC:** NJM-185019 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 3 Exclusion
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185019, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify Exclusion rules using foreign-language folder names/paths (Arabic, Chinese, Korean,
Russian) are accepted, displayed correctly, and the named folders are excluded from the backup.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\ForeignFolders\` — `مجلد_عربي\file.txt` (Arabic),
  `中文文件夹\file.txt` (Chinese), `한국어_폴더\file.txt` (Korean), `Русская_папка\file.txt`
  (Russian), plus one plain `keep.txt` at the top level (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**, source mapping = `ForeignFolders` (parent), + Exclusion listing all four
  folder names, one per line

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `ForeignFolders` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping
  `C:/TestData_ForFLB/IncludeExclude/ForeignFolders` (FOLDER), exclusion = the 4 foreign-language
  folder names (one per line: `مجلد_عربي`, `中文文件夹`, `한국어_폴더`, `Русская_папка`),
  `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-185019` → `saveJob`.
  **EXECUTED 2026-07-15:** job id 127. Unicode round-tripped correctly through the RPC
  (`getJob` echoed the identical 4 folder names, newline-separated, byte-for-byte).
### Step 2 — Run — R5 (solo, no other job on PM-3 concurrently). **EXECUTED:** `lrState:OK`,
  `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-150`, `SAVEPOINT-768`,
  `consumed:4` bytes = exactly `keep.txt`'s content, confirming all 4 foreign folders excluded).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\ForeignFolders\` →
  **CONFIRMED** only `keep.txt` present; all 4 foreign-language-named folders absent entirely
  (bare-folder-name exclusion behaves the same as the plain-ASCII case in NJM-182429 — the
  excluded folder disappears from the tree rather than just emptying).

## Cleanup
- **PASS** — job 127 removed (`JobManagement.remove [127, false]`).

## Expected final verdict
**PASS.** All 4 foreign-language-named folders excluded; `keep.txt` backed up correctly.
Unicode folder names in the Exclusion textarea are accepted, transmitted, and matched correctly.
