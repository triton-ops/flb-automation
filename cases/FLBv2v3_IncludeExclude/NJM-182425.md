# NJM-182425 — [FLB v3] FLB Job Wizard - Step 2 Inclusion - Include files by file name (FLB-11)

- **Source TC:** NJM-182425 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2 Inclusion
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-182425, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify that a File-name inclusion rule (`report*`) backs up only files whose name matches,
skipping the rest.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\ByName\` — `Q1Report.docx`, `Report_final.pdf`,
  `budget.xlsx`, `notes.txt` (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Inclusion (`enabledSourceItemsInclusion=true`, `sourceItemsInclusion="report*"`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `ByName` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/IncludeExclude/ByName`
  (FOLDER), inclusion rule `report*`, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-182425` → `saveJob`. **EXECUTED 2026-07-15:** job id 115.
### Step 2 — Run — R5. **EXECUTED:** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-139`, `SAVEPOINT-757`).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\ByName\` →
  **CONFIRMED** only `Report_final.pdf` present; `Q1Report.docx`, `budget.xlsx`, `notes.txt`
  absent. Resolves the live-semantics question: `report*` is a **case-insensitive prefix**
  match — `Report_final.pdf` starts with "Report" (matches), `Q1Report.docx` does not start
  with "Report" (correctly excluded despite containing the substring).

## Cleanup
- **PASS** — job 115 removed (`JobManagement.remove [115, false]`).

## Expected final verdict
**PASS.** Only the name-prefix-matching file (`Report_final.pdf`) is present in the recovery
point; `report*` confirmed as a case-insensitive prefix rule, not a substring/contains rule.
