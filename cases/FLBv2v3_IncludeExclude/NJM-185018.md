# NJM-185018 — [FLB v3] FLB Job Wizard - Step 2/3 - Unicode characters supported (FLB-11/FLB-12)

- **Source TC:** NJM-185018 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2/3
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185018, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify Unicode characters (accented Latin + CJK) are accepted in Inclusion/Exclusion rules,
displayed correctly (no mojibake/truncation) after navigating away and back, and filter as
intended when the job runs.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\Unicode\` — `café_résumé.txt`,
  `日本語ファイル.txt`, folder `文件夹\inside.txt` (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Inclusion/Exclusion rule containing Unicode text (e.g. exclude `日本語ファイル.txt`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `Unicode` fixture seeded on PM-3 — R0

## Steps
### Step 1/2 — UI round-trip (wizard entry + navigate-away-and-back) — **not independently
  driven via browser in this pass**; the RPC-level round-trip (Step 3) already proves the
  server accepts and persists the Unicode text byte-for-byte (`getJob` echoed back the identical
  `日本語ファイル.txt` string). A dedicated UI-navigation click-through is deferred to the
  UI-only validation batch (task #9) if a stronger UI-specific check is wanted later.
### Step 3 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/IncludeExclude/Unicode`
  (FOLDER), exclusion `日本語ファイル.txt`, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-185018` → `saveJob`. **EXECUTED 2026-07-15:** job id 132. Unicode round-tripped
  correctly through the RPC.
### Step 4 — Run — R5; Poll — R6 · **EXECUTED:** `lrState:OK`, `lrVmOk:1`
  (`BACKUP_OBJECT-155`, `SAVEPOINT-773`, `consumed:16` bytes).
### Step 5 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\Unicode\` →
  **CONFIRMED** `café_résumé.txt` (7B) and folder `文件夹` present; `日本語ファイル.txt` (11B)
  correctly excluded. **Browsing INTO `文件夹` returned empty** — this reproduces the
  nested-folder FLR-browse defect (recipes.md R4 note), this time with an **Exclusion-only**
  filter (broadening the earlier NJM-182426 finding, which used Inclusion). Confirmed via
  byte-count instead: `winrm_run Get-ChildItem` shows `café_résumé.txt`=7B, `日本語ファイル.txt`=11B,
  `inside.txt`=9B; savepoint `consumed:16` = 7+9 exactly, proving `inside.txt` **was** captured
  correctly even though FLR can't browse to it.

## Cleanup
- **PASS** — job 132 removed (`JobManagement.remove [132, false]`).

## Expected final verdict
**PASS** (top-level content verified via FLR browse; nested `inside.txt` verified via
byte-count due to the known nested-folder FLR-browse limitation). Unicode entries (accented
Latin + CJK) are accepted, round-trip correctly through the RPC, and filter as intended —
`日本語ファイル.txt` excluded, `café_résumé.txt` and `文件夹/inside.txt` backed up.
