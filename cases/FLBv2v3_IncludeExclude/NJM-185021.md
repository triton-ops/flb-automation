# NJM-185021 — [FLB v3] FLB Job Wizard - Inclusion + Exclusion - Overrule Example B (include keep.txt, exclude *.txt) (FLB-11/FLB-12)

- **Source TC:** NJM-185021 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2+3 combined
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185021, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify: Include `keep.txt` (specific) + Exclude `*.txt` (wildcard) → NO `.txt` files are backed
up at all, including `keep.txt` (the broader exclusion overrules the specific inclusion).

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\TxtOverruleB\` — `keep.txt`, `a.txt`, `b.txt`
  (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Inclusion (`keep.txt`) + Exclusion (`*.txt`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `TxtOverruleB` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping
  `C:/TestData_ForFLB/IncludeExclude/TxtOverruleB` (FOLDER), inclusion `keep.txt`, exclusion
  `*.txt`, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-185021` → `saveJob`.
  **EXECUTED 2026-07-15:** job id 130.
### Step 2 — Run — R5. **EXECUTED:** the job **succeeded** (`lrState:OK`, `lrVmOk:1`) even
  though the effective item set is empty — NBR does not treat "zero items matched" as a failure.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-153`, `SAVEPOINT-771`,
  `consumed:0` bytes — confirms nothing was captured at all).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\TxtOverruleB\` →
  **CONFIRMED** empty listing (0 items) — no `.txt` files present, including `keep.txt`.

## Cleanup
- **PASS** — job 130 removed (`JobManagement.remove [130, false]`).

## Expected final verdict
**PASS.** No `.txt` files (including the explicitly-included `keep.txt`) are present — the
broader `*.txt` exclusion overrules the specific `keep.txt` inclusion, and the job succeeds
cleanly with an empty recovery point rather than failing.
