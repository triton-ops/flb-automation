# NJM-185017 — [FLB v3] FLB Job Wizard - Step 2/3 - Entry rules: one item per line, empty line ignored, duplicate lines treated as one (FLB-11/FLB-12)

- **Source TC:** NJM-185017 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2/3
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185017, author=tri.ton, date=2026-07-15, product=FLB, status=PENDING

## Objective
Verify the Inclusion textarea: Enter starts a new line (one parameter per line), an empty line is
ignored (no error), and duplicate entries are treated as a single parameter. **UI-behavior check.**

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`) — any reachable machine
- No specific test data required

## Preconditions
- [ ] PM-3 present & OK — R1

## Steps
### Step 1 — Open the FLB wizard, advance to Step 2 - Inclusion, enable it.
### Step 2 — Type `*.docx` then press Enter → confirm cursor moves to a new line.
### Step 3 — Leave a line empty between entries → confirm no error, box stays valid.
### Step 4 — Add `*.pdf` on two separate lines (line 2 and line 4), validate/save → confirm the
  product treats them as a single parameter (no duplicate-related error, and — if inspectable —
  only one `*.pdf` entry persists in the saved job's `sourceItemsInclusion`).

## Cleanup
- No job created — cancel the wizard without saving.

## Expected final verdict
PASS if Enter creates one-parameter-per-line, empty lines are silently ignored, and duplicate
lines collapse to one parameter.
