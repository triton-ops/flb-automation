# NJM-185015 — [FLB v3] FLB Job Wizard - Step 2/3 - Parameter box limits: 5000 chars total, 255 (Win) / 4095 (Linux) per path (FLB-11/FLB-12)

- **Source TC:** NJM-185015 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2/3
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185015, author=tri.ton, date=2026-07-15, product=FLB, status=PENDING

## Objective
Verify the Inclusion/Exclusion textarea caps total input at 5000 characters, and rejects a single
path exceeding 255 chars on Windows / 4095 chars on Linux. **UI-behavior/validation check — needs
BOTH a Windows and a Linux source to cover both path-length limits.**

## Fixtures used
- Source A: `windows-src` **PM-3** (`Window11`) — for the 255-char Windows path limit
- Source B: `linux-src` **PM-2** (`Linux_16.84`) — for the 4095-char Linux path limit
- No specific test data required — textarea input validation only

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] PM-2 present & OK — R1

## Steps
### Step 1 (PM-3) — Open the FLB wizard, advance to Step 2 - Inclusion, enable it.
### Step 2 — Paste exactly 5000 characters → expect all accepted.
### Step 3 — Attempt to add a 5001st character → expect rejected/capped at 5000.
### Step 4 — Enter a 257-character Windows path → expect rejected citing the 255-char limit.
### Step 5 (PM-2) — Repeat with a 4097-character Linux path → expect rejected citing the
  4095-char limit.
### Step 6 — Screenshot each validation state as evidence.

## Cleanup
- No job created — cancel the wizard without saving.

## Expected final verdict
PASS if the 5000-char total cap and the 255/4095-char per-path limits are both enforced with a
clear validation message.
