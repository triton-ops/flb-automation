# NJM-185014 — [FLB v3] FLB Job Wizard - Step 2/3 - Wildcards allowed in filename but not in path (FLB-11/FLB-12)

- **Source TC:** NJM-185014 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2/3
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185014, author=tri.ton, date=2026-07-15, product=FLB, status=PENDING

## Objective
Verify wildcards are accepted in the filename segment of a path (`C:\Data\Backup\*.ext`) but
rejected when used in an intermediate path segment (`C:\Data\*\Backup\*.ext`); full exact paths
with either `/` or `\` delimiters are accepted. **UI-behavior/validation check.**

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`) — any reachable machine
- No specific test data required — textarea input validation only

## Preconditions
- [ ] PM-3 present & OK — R1

## Steps
### Step 1 — Open the FLB wizard, advance to Step 2 - Inclusion (or Step 3 - Exclusion), enable it.
### Step 2 — Enter `C:\Data\Backup\*.ext` → expect accepted (no red box).
### Step 3 — Enter `C:\Data\*\Backup\*.ext` → expect rejected (red box, "Invalid parameters").
### Step 4 — Enter `C:\Data\Backup\Filename.ext` and `/data/backup/Filename.ext` (both delimiters)
  → expect both accepted.
### Step 5 — Screenshot each state as evidence.

## Cleanup
- No job created — cancel the wizard without saving.

## Expected final verdict
PASS if filename-only wildcards are accepted, path-segment wildcards are rejected, and both path
delimiters are accepted for exact paths.
