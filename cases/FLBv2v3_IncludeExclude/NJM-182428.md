# NJM-182428 — [FLB v3] FLB Job Wizard - Step 3 Exclusion - Exclude files by file name (FLB-12)

- **Source TC:** NJM-182428 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 3 Exclusion
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-182428, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify that a File-name exclusion rule (`*tmp*`) excludes matching files, keeping the rest.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\NameExclude\` — `notes.txt`,
  `backup_tmp_file.txt`, `another_tmp.log` (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Exclusion (`enabledSourceItemsExclusion=true`, `sourceItemsExclusion="*tmp*"`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `NameExclude` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping `C:/TestData_ForFLB/IncludeExclude/NameExclude`
  (FOLDER), exclusion rule `*tmp*`, `targetStorageVid=BACKUP_REPOSITORY-2`, name
  `AUTO_FLB_NJM-182428` → `saveJob`. **EXECUTED 2026-07-15:** job id 124. Built/run alongside job
  123 (same PM-3 source) concurrently at first — both stalled (see NJM-182427's evidence); fixed
  by re-running solo after 123 completed. See `no-concurrent-jobs-same-source` lesson.
### Step 2 — Run — R5 (solo). **EXECUTED:** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-149`, `SAVEPOINT-767`).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\NameExclude\` →
  **CONFIRMED** only `notes.txt` present; `backup_tmp_file.txt`/`another_tmp.log` absent.

## Cleanup
- **PASS** — job 124 removed (`JobManagement.remove [124, false]`).

## Expected final verdict
**PASS.** Name-matching (`*tmp*`) files excluded; `notes.txt` backed up correctly.
