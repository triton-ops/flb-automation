# NJM-182430 — [FLB v3] FLB Job Wizard - Inclusion + Exclusion - Combined rules precedence

- **Source TC:** NJM-182430 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2+3 combined
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-182430, author=tri.ton, date=2026-07-15, product=FLB, status=EXECUTED, verdict=PASS

## Objective
Verify that when Inclusion (`*.docx`) and Exclusion (a specific `.docx` file) are both set,
exclusion takes precedence within the included set.

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`)
- Test data: `C:\TestData_ForFLB\IncludeExclude\CombinedPrecedence\` — `report1.docx`,
  `report2.docx`, `draft.docx` (to be seeded)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c** + Inclusion (`*.docx`) + Exclusion (`draft.docx`)

## Preconditions
- [ ] PM-3 present & OK — R1
- [ ] Onboard repo online — R3
- [ ] `CombinedPrecedence` fixture seeded on PM-3 — R0

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, mapping
  `C:/TestData_ForFLB/IncludeExclude/CombinedPrecedence` (FOLDER), inclusion `*.docx`, exclusion
  `draft.docx`, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-182430` → `saveJob`.
  **EXECUTED 2026-07-15:** job id 128.
### Step 2 — Run — R5. **EXECUTED:** `lrState:OK`, `lrVmOk:1`.
### Step 3 — Poll — R6 · **CONFIRMED** `lrState:OK` (`BACKUP_OBJECT-151`, `SAVEPOINT-769`).
### Step 4 — Verify — R7 · FLR browse `C:\TestData_ForFLB\IncludeExclude\CombinedPrecedence\` →
  **CONFIRMED** `report1.docx`/`report2.docx` present; `draft.docx` absent.

## Cleanup
- **PASS** — job 128 removed (`JobManagement.remove [128, false]`).

## Expected final verdict
**PASS.** All `.docx` present except `draft.docx` — exclusion correctly overrules inclusion
within the included set.
