# NJM-185016 — [FLB v3] FLB Job Wizard - Step 2/3 - Invalid parameter highlights box red with 'Invalid parameters' message (FLB-11/FLB-12)

- **Source TC:** NJM-185016 (Xray, part of Test Execution NJM-182720)
- **Feature:** File-Level Backup (physical machine) — Job Wizard Step 2/3
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-185016, author=tri.ton, date=2026-07-15, product=FLB, status=PENDING

## Objective
Verify entering an invalid parameter (alongside a valid one) turns the whole textarea red, shows
an "Invalid parameters: <name>" message naming the offending line(s), and blocks Apply/Next until
fixed. **UI-behavior/validation check.**

## Fixtures used
- Source: `windows-src` **PM-3** (`Window11`) — any reachable machine
- No specific test data required — textarea input validation only

## Preconditions
- [ ] PM-3 present & OK — R1

## Steps
### Step 1 — Open the FLB wizard, advance to Step 2 - Inclusion, enable it.
### Step 2 — Enter `*.docx` on one line and `My file.xlsx` (space in name — invalid per spec) on
  another.
### Step 3 — Confirm the textarea is highlighted red and a message names `My file.xlsx` as invalid.
### Step 4 — Confirm Apply/Next is blocked while invalid.
### Step 5 — Remove the invalid line; confirm the red highlight clears and Apply/Next re-enables.
### Step 6 — Screenshot each state as evidence.

## Cleanup
- No job created — cancel the wizard without saving.

## Expected final verdict
PASS if the red-box + message + Apply/Next-blocking behavior all match, and clearing the invalid
entry restores normal state.
