# NJM-68933 — [FLB v1] FLB - Functional (Linux) - Verify Backup of EXT4 File System

- **Source TC:** NJM-68933 (Xray, FLB/Share, Critical P2, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68933, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE if the Linux source root/data volume is EXT4 (typical); else BLOCKED(env).

## Objective
Verify FLB correctly reads and backs up data from an **EXT4** volume on a Linux physical machine and
produces a valid recovery point (spec: Linux EXT4 supported; volumes treated as folders).

## Fixtures used
- Source: linux-src **PM-2** (`Linux_16.84`, `/TestData_ForFLB`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68933`

## Preconditions
- [ ] PM-2 present & OK — **R1**
- [ ] **BLOCKED guard:** confirm an **EXT4** volume/path exists on PM-2 (remoting `df -T /TestData_ForFLB`
      shows `ext4`). If the data path is not ext4 → BLOCKED(env): "requires an EXT4 volume".
- [ ] Onboard repo id 2 online — **R3**

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-2`/`Linux_16.84`, MappingBuilder `mappings=[{NORMAL,"/TestData_ForFLB/ft_code",FOLDER}]` (on the EXT4 volume), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68933` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Verify — R7 · getSavepoints; FLR browse + EXPORT→SHA-256 vs `manifest-linux.sha256`.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK` and recovered EXT4-volume files are byte-identical to the Linux manifest.
