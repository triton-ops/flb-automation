# NJM-70016 — [FLB v2] FLB - Functional - Verify User Story: Backup Specific Individual Files (US4)

- **Source TC:** NJM-70016 (Xray, FLB/Share, Blocker P1, flb-v2)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-70016, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (file-level selection verified on build 106315)

## Objective
Verify US4: a user can select **specific individual files** (not folders/volumes), the job succeeds,
and the recovery point contains **only** those files — sibling files in the same folders are excluded
(spec: v2 file selection via `sourceIdentifierType=FILE`).

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB`)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-70016`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**.
- [ ] **BLOCKED guard:** if PM-3/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select **individual FILES** with
  `sourceIdentifierType:"FILE"`, e.g. `mappings=[{NORMAL,"C:/TestData_ForFLB/ft_pdf/sample_pdf.pdf",FILE},
  {NORMAL,"C:/TestData_ForFLB/ft_code/sample_code.py",FILE}]`, `targetStorageVid=BACKUP_REPOSITORY-2`,
  name `AUTO_FLB_NJM-70016` → `saveJob`.
- **Expect:** `{OK, jobId}`; mappings are FILE entries only.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Verify scope — R7 · FLR `list` shows **only** the selected files (e.g. `ft_pdf/` contains
  only `sample_pdf.pdf`), siblings excluded. Optional EXPORT→SHA-256 vs manifest for the picked files.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK` and the recovery point contains exactly the individually-selected files, no siblings.
