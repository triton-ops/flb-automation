# NJM-123123 — [FLB v1] FLB - Functional - Verify Backup to Backblaze B2 Repository (Windows & Linux)

- **Source TC:** NJM-123123 (Xray, FLB/Share, Critical P2, label flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123123, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED(env) — no **Backblaze B2** repository on nbr-84 (one exists on nbr-5, not this FLB box).

## Objective
Verify FLB backs up mixed files from Windows & Linux sources to a **Backblaze B2** repository
(full + incremental) and FLR-recovers byte-identically (spec: Backblaze B2 is a supported
S3-compatible destination, Compliance-only immutability).

## Fixtures used
- Sources: windows-src **PM-3**, linux-src **PM-2**
- Repository: **Backblaze B2** — `BACKUP_REPOSITORY-<id>` *(REQUIRED PREREQUISITE — not present on nbr-84)*
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-123123`

## Preconditions
- [ ] **BLOCKED guard (decisive):** `getBackupRepositories` must contain a repo of **type
      `BACKBLAZE`/B2**, state OK. None on nbr-84 → **BLOCKED(env): "requires a Backblaze B2
      repository on nbr-84"** and stop.
- [ ] PM-2 & PM-3 present & OK — **R1**

## Steps (execute only once the B2 repo exists)
### Step 1 — Build — R4c: two sources, folder mapping each, `targetStorageVid`=B2 repo VID, name `AUTO_FLB_NJM-123123` → `saveJob`.
### Step 2 — Full run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`, `lrVmOk=2`; RP in B2.
### Step 4 — Incremental → run again.
### Step 5 — Verify — R7 · FLR export → SHA-256 vs manifests.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if full+incremental reach `lrState:OK` (lrVmOk=2) and recovered files match manifests.
Otherwise **BLOCKED(env)** until a Backblaze B2 repository is provisioned on nbr-84.
