# NJM-85758 — [FLB v1] FLB - Reporting - Verify FLB Jobs are Included in NBR Usage Data

- **Source TC:** NJM-85758 (Xray, FLB/Share, Critical P2, flb-v1; linked closed NJM-87565/135873/135875)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-85758, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — needs ≥1 successful FLB job; usage-data via RPC (introspect) or UI page.

## Objective
Verify FLB jobs and their protected workload (source machines, protected data size/item count) are
reflected in **NBR Usage Data** with correct values (spec: FLB licensed as Physical Machines Support;
reporting/usage-data integration).

## Fixtures used
- A successful FLB job with ≥1 recovery point (build via R4c if none), source **PM-3**
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-85758`

## Preconditions
- [ ] ≥1 FLB job completed successfully with protected data — build+run via R4c if needed.
- [ ] **BLOCKED guard:** introspect for the usage-data RPC — `describe_method` on the licensing/usage
      service (e.g. `LicensingManagement`/usage endpoint). If no usage RPC is exposed → **PARTIAL**:
      verify via UI (Settings → Licensing / Usage Data page) + screenshot (R8).

## Steps
### Step 1 — Ensure FLB job — R4c create+run → `lrState:OK`, RP present (protected data in repo).
### Step 2 — Open/generate Usage Data — via the introspected usage RPC or the UI Usage Data page.
### Step 3 — Verify — locate the FLB job in the usage output; its protected workload (source machine(s),
  protected data size / item count) is present with **correct** values.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]` (if the job was created for this test). On FAIL — keep + record.

## Expected final verdict
PASS if the FLB job and its protected workload appear in NBR Usage Data with correct counts/sizes.
