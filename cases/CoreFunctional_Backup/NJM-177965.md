# NJM-177965 — [FLB] Reliability - Volume snapshot creation failure falls back to direct volume read

- **Source TC:** NJM-177965 (Xray, FLB/Share, Blocker P1, label FLB-Share-reliability)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-177965, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED / manual — the framework cannot reliably FORCE VSS/volume-snapshot failure.

## Objective
Verify that when volume (VSS) snapshot creation **fails/is unsupported**, FLB discovers volumes and
reads data **directly** from them, the backup still completes successfully, and any created snapshots
are removed afterward (spec: uses snapshots when possible; falls back to direct volume read).

## Fixtures used
- Source: windows-src **PM-3** (VSS) with snapshot creation forced to fail
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-177965`

## Preconditions
- [ ] **BLOCKED guard (decisive):** requires a source configured so VSS snapshot creation fails or is
      unsupported (e.g. VSS service disabled / unsupported volume). This is a **manual environment
      setup** — report **BLOCKED(manual): "requires a source with VSS snapshot forced to fail"** unless
      arranged out-of-band.
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**

## Steps (once the fail condition is arranged)
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder a volume/folder scope, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-177965` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** `lrState:OK` (no hard failure from snapshot unavailability); job log shows direct-volume-read fallback.
### Step 3 — Verify — R7 · FLR browse/checksum confirms data captured; confirm no leftover snapshots on the source (remoting `vssadmin list shadows`).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if the backup succeeds via direct volume read when snapshots fail, data is captured, snapshots cleaned up.
Otherwise **BLOCKED(manual)** until the snapshot-failure condition is arranged.
