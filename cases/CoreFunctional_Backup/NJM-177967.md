# NJM-177967 — [FLB] Reliability - Non-root items missing at run time are skipped and job continues

- **Source TC:** NJM-177967 (Xray, FLB/Share, Blocker P1, label FLB-Share-reliability)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-177967, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — automatable via remoting (remove sub-items) + RPC (job state/notifications).

## Objective
Verify that when some **non-root sub-items** are missing at run time, they are **skipped**, the job
**continues and succeeds**, and one **error346** notification per server listing the skipped items
appears after the run (spec: skip-and-continue for non-root items).

## Fixtures used
- Source: windows-src **PM-3** (or PM-2) — a root folder with sub-items, some removed pre-run
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-177967`

## Preconditions
- [ ] Source present & OK — **R1**; Onboard repo online — **R3**
- [ ] **Setup (remoting):** create a root folder with several sub-items; note which will be removed.
- [ ] **BLOCKED guard:** if setup not possible → PARTIAL/BLOCKED, note why.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder the root folder (FOLDER), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-177967` → `saveJob`.
### Step 2 — Remove sub-items — delete some non-root sub-items on the source (remoting).
### Step 3 — Run — R5 (`runType:ALL`) · **Expect:** job **Successful** (`lrState:OK`); missing sub-items skipped.
### Step 4 — Notification — `ActivityManagement`/dashboard shows **one error346** per server listing the skipped items.
### Step 5 — Verify — R7 · FLR: present sub-items backed up; removed ones absent.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if the job succeeds with missing non-root items skipped and the error346 skipped-items notification present.
