# NJM-67682 — [FLB v1] FLB - Functional - Verify Backup to an Amazon EC2 Repository

- **Source TC:** NJM-67682 (Xray, FLB/Share, Critical P2, label flb-v1; linked closed defect NJM-77918)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-67682, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED(env) — no **Amazon EC2** repository on nbr-84.

## Objective
Verify FLB backs up a folder of varied file types (.pdf/.xml/.json/.docx/.sys/.jpg/.mp4) to an
**Amazon EC2** repository and produces a recovery point containing all file types (spec: Amazon EC2
is a supported FLB destination).

## Fixtures used
- Source: windows-src **PM-3** (or linux-src PM-2), folder with the 7 varied types
- Repository: **Amazon EC2** — `BACKUP_REPOSITORY-<id>` *(REQUIRED PREREQUISITE — not present)*
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-67682`

## Preconditions
- [ ] **BLOCKED guard (decisive):** `getBackupRepositories` must contain an **Amazon EC2** repo,
      state OK. None on nbr-84 → **BLOCKED(env): "requires an Amazon EC2 repository"** and stop.
- [ ] Source present & OK — **R1**; folder with the 7 file types present.

## Steps (execute only once the EC2 repo exists)
### Step 1 — Build — R4c: `sourceVid`=PM-3, folder mapping (mixed types), `targetStorageVid`=EC2 repo VID, name `AUTO_FLB_NJM-67682` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Verify — R7 · getSavepoints; FLR browse confirms RP contains **all 7 file types**.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if the job reaches `lrState:OK` and the RP contains every selected file type.
Otherwise **BLOCKED(env)** until an Amazon EC2 repository is provisioned.
