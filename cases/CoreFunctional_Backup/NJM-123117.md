# NJM-123117 — [FLB v1] FLB - Functional - Verify Backup to Amazon S3 Repository (Windows & Linux)

- **Source TC:** NJM-123117 (Xray, FLB/Share, Critical P2, label flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123117, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED(env) — no native **Amazon S3** repository on nbr-84 (Ceph_S3 id 8 is
  S3-*compatible*, NOT Amazon S3). Runnable once an Amazon S3 repo is added.

## Objective
Verify FLB backs up mixed files from Windows & Linux sources to an **Amazon S3** repository
(full + incremental) and FLR-recovers byte-identically (spec: Amazon S3 is a supported destination).

## Fixtures used
- Sources: windows-src **PM-3**, linux-src **PM-2**
- Repository: **Amazon S3** — `BACKUP_REPOSITORY-<id>` *(REQUIRED PREREQUISITE — not present)*
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-123117`

## Preconditions
- [ ] **BLOCKED guard (decisive):** `BackupManagement.getBackupRepositories` must contain a repo of
      **type `AMAZON`/Amazon S3**, state OK. On nbr-84 today there is none → **report BLOCKED(env):
      "requires an Amazon S3 repository on nbr-84"** and stop.
- [ ] PM-2 & PM-3 present & OK — **R1**

## Steps (execute only once the Amazon S3 repo exists)
### Step 1 — Build the job — R4c
- Two source objects (PM-3, PM-2); MappingBuilder a folder on each; RepositoryBuilder
  `targetStorageVid` = the Amazon S3 repo VID; name `AUTO_FLB_NJM-123117` → `saveJob`.
### Step 2 — Full run — R5 (`runType:ALL`) · **Expect:** accepted.
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`, `lrVmOk=2`; RP in Amazon S3.
### Step 4 — Incremental — add data → run again · **Expect:** new RP, `lrState:OK`.
### Step 5 — Verify — R7 · FLR export → SHA-256 vs manifests (both hosts).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if full+incremental reach `lrState:OK` (lrVmOk=2) and recovered files match manifests.
Otherwise **BLOCKED(env)** until an Amazon S3 repository is provisioned.
