# NJM-85749 — [FLB v1] FLB - Platform Support - Verify Backup Using an Amazon EC2-based Transporter

- **Source TC:** NJM-85749 (Xray, FLB/Share, Critical P2, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-85749, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED(env) — no **Amazon EC2-based transporter** on nbr-84
  (transporters present: Onboard `TRANSPORTER-1`, Linux_Tranporter_15.62 `TRANSPORTER-7`).

## Objective
Verify an FLB job routed through an **Amazon EC2-based transporter** completes and the data recovers
intact (spec: transporter/platform support).

## Fixtures used
- Source: physical machine **PM-3/PM-2**
- Transporter: **Amazon EC2-based** *(REQUIRED PREREQUISITE — not present)*
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-85749`

## Preconditions
- [ ] **BLOCKED guard (decisive):** an Amazon **EC2-based transporter** must be present & online
      (check `InventoryManagement`/transporter list). None on nbr-84 → **BLOCKED(env): "requires an
      Amazon EC2-based transporter"** and stop.
- [ ] Source present & OK — **R1**

## Steps (once the EC2 transporter exists)
### Step 1 — Build — R4c: `sourceVid=PM-3`, folder mapping, BackupOptionsBuilder pin the data-transfer transporter to the EC2-based one (`options.transporterType`/`sourceTransporterVid`/`targetTransporterVid`), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-85749` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** `lrState:OK`, data routed via the EC2-based transporter.
### Step 3 — Verify — R7 · FLR-recover a file → EXPORT→SHA-256 vs manifest (integrity via EC2 transporter).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if `lrState:OK` using the EC2-based transporter and recovered data matches source.
Otherwise **BLOCKED(env)** until an Amazon EC2-based transporter is provisioned.
