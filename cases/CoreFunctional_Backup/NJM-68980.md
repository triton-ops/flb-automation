# NJM-68980 — [FLB v2] FLB - Functional - Verify Handling of Symbolic and Hard Links

- **Source TC:** NJM-68980 (Xray, FLB/Share, Major P3, flb-v2; linked closed defect NJM-134697)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68980, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — needs symbolic + hard links on the Linux source (host setup via remoting).

## Objective
Verify FLB handling of **symbolic links** (skipped) and **hard links** (files sharing one inode) on
Linux: the job logs the handling clearly, no unexpected errors, and recovery reflects the documented
behavior (spec: symlinks skipped; hard-link behavior observed).

## Fixtures used
- Source: linux-src **PM-2** — folder with a symlink + two hard-linked files (same inode)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68980`

## Preconditions
- [ ] PM-2 present & OK — **R1**; Onboard repo online — **R3**
- [ ] **Setup (remoting `flb-linux`):** `ln -s target.txt link.txt`; `ln file_a file_b` (hard link);
      record paths/inodes/checksums.
- [ ] **BLOCKED guard:** if links cannot be created → PARTIAL/BLOCKED, note why.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-2`, MappingBuilder the folder with links, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68980` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** `lrState:OK`; job log indicates link handling.
### Step 3 — Verify — R7 · FLR `list`: symlink skipped; hard-linked files present per documented behavior; EXPORT→SHA-256 for real/hard-linked files; no corruption/missing files.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK`, symbolic links skipped, hard links handled per behavior, no link-induced errors/corruption.
