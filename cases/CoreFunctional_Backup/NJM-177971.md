# NJM-177971 — [FLB] Reliability - Symbolic links among selected folders/volumes are skipped

- **Source TC:** NJM-177971 (Xray, FLB/Share, Blocker P1, label FLB-Share-reliability)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-177971, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — needs symbolic links in the selected scope (host setup via remoting).

## Objective
Verify **symbolic links** among selected folders/volumes are **skipped** (not followed, not backed up)
while real folders/files back up, and links cause no errors (spec: symlinks skipped — only path info,
not target data).

## Fixtures used
- Source: linux-src **PM-2** (or windows-src PM-3) — a folder containing symlinks + real files
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-177971`

## Preconditions
- [ ] Source present & OK — **R1**; Onboard repo online — **R3**
- [ ] **Setup (remoting `flb-linux`):** in a test folder create a symlink → target file
      (`ln -s target.txt link.txt`) alongside real files; record real-file checksums.
- [ ] **BLOCKED guard:** if symlinks cannot be created in scope → PARTIAL/BLOCKED, note why.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-2`, MappingBuilder the folder containing symlinks + real files, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-177971` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** `lrState:OK`, no errors from links.
### Step 3 — Verify — R7 · FLR `list`: symlink entries **skipped/absent**; real files present; EXPORT→SHA-256 vs recorded checksums for real files.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK`, symbolic links are skipped (not backed up), and real files recover intact with no link-induced errors.
