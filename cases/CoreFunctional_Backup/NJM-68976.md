# NJM-68976 — [FLB v2] FLB - Functional - Verify Behavior with Filesystem-Encrypted Items (Expected to Skip)

- **Source TC:** NJM-68976 (Xray, FLB/Share, Major P3, flb-v2; linked closed defect NJM-134358)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68976, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — needs EFS-encrypted files on the Windows source (host setup via remoting).

## Objective
Verify Windows **EFS filesystem-encrypted** files are **skipped** (not errors) while ordinary files
back up; RP excludes the EFS files (spec: filesystem-encrypted files skipped; skip-and-continue).

## Fixtures used
- Source: windows-src **PM-3** — folder with EFS-encrypted + ordinary files
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68976`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**
- [ ] **Setup (remoting `win11`):** create a folder with EFS files (`cipher /E secret.docx`) + ordinary
      `plain.txt`; record names/checksums.
- [ ] **BLOCKED guard:** if EFS cannot be set up on PM-3 → PARTIAL/BLOCKED, note why.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder the mixed EFS folder, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68976` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** `lrState:OK` (not failed by EFS files).
### Step 3 — Skip check — job events/activities show EFS files **skipped** (not errors).
### Step 4 — Verify — R7 · FLR `list`: EFS files **absent**; ordinary files present; EXPORT→SHA-256 vs recorded checksums for ordinary files.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record. (Also remove the EFS test folder.)

## Expected final verdict
PASS if `lrState:OK`, EFS files reported skipped and absent from the RP, ordinary files recover intact.
