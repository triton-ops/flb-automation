# NJM-177966 — [FLB] Reliability - Root folder renamed/moved/deleted at run time fails job with ict45

- **Source TC:** NJM-177966 (Xray, FLB/Share, Blocker P1, label FLB-Share-reliability)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-177966, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — job/error path automatable via remoting+RPC; wizard-state check needs the Playwright POM.

## Objective
Verify that if a selected **root folder** is renamed/moved/deleted before the run, the job **fails with
ict45** ("Root folder could not be found…"), and on job edit the missing folder still appears in
**Selected items** until removed (but not in the Select Items dialog) (spec: missing-root handling).

## Fixtures used
- Source: windows-src **PM-3** — a dedicated root folder that will be removed
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-177966`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**
- [ ] **Setup (remoting `win11`):** create a dedicated root folder `C:\FLB_root_delete\` with a file.
- [ ] **BLOCKED guard:** if the folder can't be created/removed → PARTIAL/BLOCKED, note why.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder `mappings=[{NORMAL,"C:/FLB_root_delete",FOLDER}]`, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-177966` → `saveJob`.
### Step 2 — Remove the root — delete/rename `C:\FLB_root_delete` on the source (remoting).
### Step 3 — Run — R5 (`runType:ALL`) · **Expect:** job **FAILS**; events/alarm show **ict45** ("Root folder could not be found").
### Step 4 — Wizard-state (Playwright POM, R8) — open the job for edit: the missing folder still shows in **Selected items** until removed, and is **not** listed in the Select Items dialog.

## Cleanup
- Job FAILED by design → keep + record job id (per policy). Remove the test root folder + job when done: `remove [<jobId>, false]`.

## Expected final verdict
PASS if the run fails with **ict45** and the wizard shows the missing root in Selected items until removed.
