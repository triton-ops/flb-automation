# NJM-68916 — [FLB v1] FLB - Functional (Windows) - Verify Backup from FAT16/32, NTFS, ReFS

- **Source TC:** NJM-68916 (Xray, FLB/Share, Critical P2, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68916, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — NTFS (C:) available on PM-3; **FAT16/FAT32/ReFS volumes not present**.

## Objective
Verify FLB reads and backs up data from Windows **FAT16, FAT32, NTFS, and ReFS** volumes and produces
a recovery point with data from each (spec: Windows FS support).

## Fixtures used
- Source: windows-src **PM-3** (`Windown`) — volumes formatted FAT16 / FAT32 / NTFS / ReFS
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68916`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo id 2 online — **R3**
- [ ] **BLOCKED guard (per-FS):** enumerate PM-3 volumes (remoting `Get-Volume`). NTFS (`C:`) is
      present → cover it. For each of **FAT16 / FAT32 / ReFS** not present → mark that sub-scope
      **BLOCKED(env)** ("requires a <FS> volume on PM-3") and back up only the available FS; report
      which FS were covered vs blocked. Do not fake absent FS.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder one folder per **available** FS volume, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68916` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Verify — R7 · FLR browse confirms data from each covered FS volume; checksum vs manifest.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS for the covered file systems (`lrState:OK` + data recovered); FAT16/FAT32/ReFS remain
**BLOCKED(env)** until those volumes are provisioned on PM-3.
