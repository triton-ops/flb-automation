# NJM-85746 — [FLB v1] FLB - Platform Support - Verify Backup to Amazon EC2 Repository

- **Source TC:** NJM-85746 (Xray, FLB/Share, Critical P2, flb-v1; linked closed defect NJM-114368)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-85746, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED(env) — no **Amazon EC2** repository on nbr-84. (Platform-support dup of NJM-67682.)

## Objective
Verify FLB full + incremental backup targeting an **Amazon EC2** repository, with FLR recovery
(spec: Amazon EC2 supported destination). Confirms Linux & Windows FLB to an EC2-hosted repo.

## Fixtures used
- Source: linux-src **PM-2** and/or windows-src **PM-3**
- Repository: **Amazon EC2** — `BACKUP_REPOSITORY-<id>` *(REQUIRED PREREQUISITE — not present)*
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-85746`

## Preconditions
- [ ] **BLOCKED guard (decisive):** `getBackupRepositories` must contain an **Amazon EC2** repo,
      state OK. None on nbr-84 → **BLOCKED(env): "requires an Amazon EC2 repository"** and stop.
- [ ] Source present & OK — **R1**

## Steps (once the EC2 repo exists)
### Step 1 — Build — R4c: source PM-2/PM-3, folder mapping, `targetStorageVid`=EC2 repo VID, name `AUTO_FLB_NJM-85746` → `saveJob`.
### Step 2 — Full run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Incremental — add data → run again · **Expect:** new RP; chain consistent.
### Step 5 — Verify — R7 · FLR export → SHA-256 vs manifest.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if full+incremental reach `lrState:OK` and recovered files match the manifest.
Otherwise **BLOCKED(env)** until an Amazon EC2 repository is provisioned.
