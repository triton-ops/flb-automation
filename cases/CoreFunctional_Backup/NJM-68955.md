# NJM-68955 — [FLB v1] FLB - Functional - Verify Application-Aware Processing

- **Source TC:** NJM-68955 (Xray, FLB/Share, Critical P2, flb-v1; linked closed defect NJM-130524)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68955, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED / manual — needs a VSS-aware app (SQL Server/Exchange) running on the Windows source.

## Objective
Verify **Application-Aware Processing**: with a VSS-aware app (e.g. SQL Server) running on the source,
the FLB job with app-aware enabled completes and keeps the app consistent (VSS snapshot created &
released cleanly) (spec: Options app-aware support).

## Fixtures used
- Source: windows-src **PM-3** running a VSS-aware app (SQL Server / Exchange)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68955`

## Preconditions
- [ ] **BLOCKED guard (decisive):** requires a VSS-aware application installed & running on PM-3.
      Not provisioned on nbr-84 → **BLOCKED(manual): "requires a VSS-aware app (SQL/Exchange) on the
      source"** and stop. (Framework can create/run the job; it cannot provision the app.)
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**

## Steps (once the app is present)
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder the app data folder, BackupOptionsBuilder `options.applicationAwareMode` enabled, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68955` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** `lrState:OK` with app-aware active.
### Step 3 — Verify consistency — remoting: check VSS snapshot events on the source (created & released cleanly, no errors); FLR browse confirms app data captured.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if `lrState:OK`, app-aware/VSS consistency achieved and snapshot released cleanly.
Otherwise **BLOCKED(manual)** until a VSS-aware app is available on the source.
