# NJM-123119 — [FLB v1] FLB - Functional - Verify Backup to Azure Blob Repository (Windows & Linux)

- **Source TC:** NJM-123119 (Xray, FLB/Share, Critical P2, label flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123119, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED(env) — no **Azure Blob** repository on nbr-84.

## Objective
Verify FLB backs up mixed files from Windows & Linux sources to an **Azure Blob** repository
(full + incremental) and FLR-recovers byte-identically (spec: Azure Blob is a supported destination
with version-level immutability support).

## Fixtures used
- Sources: windows-src **PM-3**, linux-src **PM-2**
- Repository: **Azure Blob** — `BACKUP_REPOSITORY-<id>` *(REQUIRED PREREQUISITE — not present)*
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-123119`

## Preconditions
- [ ] **BLOCKED guard (decisive):** `getBackupRepositories` must contain a repo of **type
      `AZURE`/Azure Blob**, state OK. None on nbr-84 today → **BLOCKED(env): "requires an Azure Blob
      repository"** and stop.
- [ ] PM-2 & PM-3 present & OK — **R1**

## Steps (execute only once the Azure Blob repo exists)
### Step 1 — Build — R4c: two sources (PM-3, PM-2), folder mapping each, `targetStorageVid`=Azure repo VID, name `AUTO_FLB_NJM-123119` → `saveJob`.
### Step 2 — Full run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`, `lrVmOk=2`; RP in Azure Blob.
### Step 4 — Incremental — add data → run again · **Expect:** new RP.
### Step 5 — Verify — R7 · FLR export → SHA-256 vs manifests.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if full+incremental reach `lrState:OK` (lrVmOk=2) and recovered files match manifests.
Otherwise **BLOCKED(env)** until an Azure Blob repository is provisioned.
