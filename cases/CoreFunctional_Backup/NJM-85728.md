# NJM-85728 — [FLB v1] FLB - Repository - Verify Integrity and Structure of Backup Data Post-Job

- **Source TC:** NJM-85728 (Xray, FLB/Share, Critical P2, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-85728, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (RPC introspection of the backup object/chain + FLR)

## Objective
Verify that after an FLB job the repository holds a correctly-structured **backup object + recovery
point(s)** (metadata + data), a full+incremental **chain** is consistent, and the stored data is
recoverable and matches source (spec: backup object structure/integrity).

## Fixtures used
- Source: windows-src **PM-3** (known folders/files)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`) or **NFS_REPO** (`-7`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-85728`

## Preconditions
- [ ] PM-3 present & OK — **R1**; repo online — **R3**
- [ ] **BLOCKED guard:** if source/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, folder mapping (known set), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-85728` → `saveJob`.
### Step 2 — Full run — R5 (`runType:ALL`) · **Expect:** `lrState:OK`; full RP created.
### Step 3 — Inspect structure — `getJob(id).objects[0].targetVid` = `BACKUP_OBJECT-<n>`; `getSavepoints [BACKUP_OBJECT-<n>,5]` → RP present (type FULL, isAccessible, metadata). Confirm repo `backupCount`/hvTypeBackupCount reflects the FLB object.
### Step 4 — Incremental — add data (remoting) → run again → `getSavepoints` shows full+incremental chain, consistent.
### Step 5 — Verify — R7 · FLR from a RP → EXPORT→SHA-256 vs `manifest-windows.sha256`; no structural errors/alarms.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if the backup object/RP/chain are well-structured & consistent and recovered data matches source.
