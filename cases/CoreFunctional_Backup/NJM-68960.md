# NJM-68960 — [FLB v1] FLB - Functional - Verify Retrieved Folders are Correctly Backed Up

- **Source TC:** NJM-68960 (Xray, FLB/Share, Blocker P1, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68960, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE

## Objective
Verify a selected **nested folder tree** (files at multiple levels) is backed up with its full
hierarchy and content intact, and recovers matching the source exactly (spec: retrieved folders
backed up; sub-folders preserved).

## Fixtures used
- Source: windows-src **PM-3** (`C:\TestData_ForFLB`) — a nested tree (e.g. `Folder_test1` with subfolders)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68960`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**
- [ ] A nested folder tree with files at multiple levels exists (seed via remoting if needed).
- [ ] **BLOCKED guard:** if source/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select the **root of the nested tree** as a FOLDER, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68960` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Verify — R7 · FLR `list` recursively confirms the full hierarchy and file content;
  EXPORT→SHA-256 vs manifest for the tree (layout + content match source).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK` and the recovered folder tree matches the source hierarchy and checksums.
