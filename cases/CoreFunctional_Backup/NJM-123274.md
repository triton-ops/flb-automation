# NJM-123274 — [FLB v1] FLB - Functional - Verify Backup from Various File Systems (NTFS, XFS, EXT4, …)

- **Source TC:** NJM-123274 (Xray, FLB/Share, Critical P2, flb-v1; cloned by NJM-128431)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123274, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — cover NTFS (PM-3) + EXT4 (PM-2); **ReFS / XFS / EXT3 / Btrfs not present**.

## Objective
Verify FLB backs up files/folders from **multiple source file systems** (Windows: NTFS, ReFS;
Linux: XFS, EXT4, EXT3, Btrfs), full + incremental, and FLR-recovers byte-identically across each
(spec: broad FS support).

## Fixtures used
- Sources: windows-src **PM-3** (NTFS) + linux-src **PM-2** (EXT4 today)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-123274`

## Preconditions
- [ ] PM-2 & PM-3 present & OK — **R1**; Onboard repo online — **R3**
- [ ] **BLOCKED guard (per-FS):** detect available FS (remoting `Get-Volume` / `df -T`, `lsblk -f`).
      Cover the present FS; for each of **ReFS / XFS / EXT3 / Btrfs** absent → mark that sub-scope
      **BLOCKED(env)** and report covered-vs-blocked. Full coverage needs those volumes provisioned.

## Steps
### Step 1 — Build — R4c: two source objects (PM-3, PM-2), one folder mapping per **available** FS volume, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-123274` → `saveJob`.
### Step 2 — Full run — R5 (`runType:ALL`) · **Expect:** `lrState:OK`.
### Step 3 — Incremental — add data on each covered FS → run again · **Expect:** new RP.
### Step 4 — Verify — R7 · FLR export → SHA-256 vs manifests for each covered FS.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS for the covered file systems (full+incremental `lrState:OK` + checksum match); the missing FS
(ReFS/XFS/EXT3/Btrfs) remain **BLOCKED(env)** until provisioned.
