# NJM-68967 — [FLB v1] FLB - Functional - Verify Backup of Locked/In-Use Files (VSS)

- **Source TC:** NJM-68967 (Xray, FLB/Share, Critical P2, flb-v1; linked closed defect NJM-83364)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68967, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — automatable: lock a file via remoting, back up (VSS default), verify recovery.

## Objective
Verify FLB backs up **locked/in-use** files via VSS snapshot (e.g. an open Office doc or DB) and the
files recover consistent/readable (spec: uses volume snapshots so in-use files are captured).

## Fixtures used
- Source: windows-src **PM-3** — a folder with a locked/in-use file
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68967`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**
- [ ] **Setup (remoting `win11`):** open/lock a file (hold an exclusive handle, e.g. a PowerShell
      process keeping a `.docx`/`.mdb` open) in the source folder; record its checksum.
- [ ] **BLOCKED guard:** if a file lock can't be held during the run → PARTIAL/BLOCKED, note why.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, folder mapping (contains the locked file), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68967` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) while the file is held open · **Expect:** `lrState:OK`; locked file captured via VSS.
### Step 3 — Verify — R7 · FLR-recover the previously-locked file → EXPORT→SHA-256 vs recorded checksum (content consistent/readable).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]` + release the lock. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK` and the locked/in-use file recovers with consistent, readable content.
