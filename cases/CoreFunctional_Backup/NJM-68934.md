# NJM-68934 — [FLB v1] FLB - Functional (Linux) - Verify Backup of XFS File System

- **Source TC:** NJM-68934 (Xray, FLB/Share, Critical P2, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68934, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL/BLOCKED(env) — needs an **XFS** volume on PM-2 (not present by default).

## Objective
Verify FLB correctly reads and backs up data from an **XFS** volume on a Linux physical machine and
produces a valid recovery point (spec: Linux XFS supported).

## Fixtures used
- Source: linux-src **PM-2** (`Linux_16.84`) — an **XFS**-formatted volume with test data
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68934`

## Preconditions
- [ ] PM-2 present & OK — **R1**
- [ ] **BLOCKED guard (decisive):** confirm an **XFS** volume with test data exists on PM-2
      (remoting `lsblk -f` / `df -T` shows `xfs`). If absent → **BLOCKED(env): "requires an XFS
      volume on PM-2"** and stop. (Setup: create/mount an XFS volume + seed files + record checksums.)
- [ ] Onboard repo id 2 online — **R3**

## Steps (once an XFS volume exists)
### Step 1 — Build — R4c: `sourceVid=PM-2`, MappingBuilder a folder on the XFS mount, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68934` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`.
### Step 4 — Verify — R7 · FLR browse + EXPORT→SHA-256 vs the XFS-volume checksums.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if `lrState:OK` and recovered XFS-volume files match their checksums.
Otherwise **BLOCKED(env)** until an XFS volume is provisioned on PM-2.
