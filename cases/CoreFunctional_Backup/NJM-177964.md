# NJM-177964 — [FLB] Reliability - Partial-failure job success when some items are skipped

- **Source TC:** NJM-177964 (Xray, FLB/Share, Blocker P1, label FLB-Share-reliability)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-177964, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL (manual-heavy setup) — requires crafting several fault conditions on the source.

## Objective
Verify **skip-and-continue**: with a mixed selection (no-permission folder, unreadable file,
prohibited-char/over-length name, EFS file, **plus ≥1 normal file**), the job **succeeds** (≥1 item
backed up), the bad items are **skipped**, and the skipped items are reported (spec: skip-and-continue;
job succeeds if ≥1 item backed up; Skipped-items report per object with full path).

## Fixtures used
- Source: windows-src **PM-3** — crafted mixed source
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-177964`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**
- [ ] **Setup (remoting `win11`) — manual/scripted:** create (a) a folder with insufficient
      permissions, (b) a file that triggers a read error, (c) a prohibited-char / over-length-path
      name, (d) an EFS-encrypted file, and (e) ≥1 normal file. Record the normal file's checksum.
- [ ] **BLOCKED guard:** conditions that cannot be injected in this environment → mark those
      sub-items PARTIAL and note them; still assert on the ones set up.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder the mixed set (bad items + ≥1 normal), `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-177964` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** job completes **Successful** (`lrState:OK`, ≥1 item backed up) — NOT failed.
### Step 3 — Skipped report — `ActivityManagement`/notifications list the skipped items (per object, full path).
### Step 4 — Verify — R7 · FLR: the normal file present + checksum match; bad items absent.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record. (Remove crafted test items.)

## Expected final verdict
PASS if the job is **Successful** with ≥1 item backed up, bad items skipped + reported, normal file recovers intact.
