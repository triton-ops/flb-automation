# NJM-85733 — [FLB v1] FLB - Repository - Verify Space Reclaim Functions Correctly with FLB Backups

- **Source TC:** NJM-85733 (Xray, FLB/Share, Critical P2, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-85733, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — needs the repository space-reclaim RPC (introspect); else UI/manual.

## Objective
Verify repository **Space Reclaim** completes with FLB backups present, frees space, and leaves the
remaining FLB recovery points intact and recoverable (spec/repository maintenance).

## Fixtures used
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`) with FLB backups (some RPs deletable/aged)
- Source: windows-src **PM-3**
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-85733`

## Preconditions
- [ ] Repo online — **R3**; ≥1 FLB backup with multiple RPs (build via R4c; run several times).
- [ ] Create reclaimable space (delete/age some RPs — `removeBackupObjectSavepoint` or retention).
- [ ] **BLOCKED guard:** introspect for the space-reclaim RPC — `describe_method` on `BackupManagement`/
      `RepositoryManagement`. If none is exposed → **PARTIAL**: run via UI (Repository → Manage → Run
      space reclaim) + screenshot (R8).

## Steps
### Step 1 — Seed — R4c create+run FLB job several times → multiple RPs; delete/age some to create reclaimable space.
### Step 2 — Run Space Reclaim — call the introspected reclaim RPC on the repo (or UI path) · **Expect:** completes successfully.
### Step 3 — Verify freed space — repo free space increased; remaining FLB RPs still consistent (`getSavepoints`).
### Step 4 — Verify — R7 · FLR from a surviving FLB RP → recovery succeeds (reclaim did not damage survivors).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if Space Reclaim completes, frees space, and surviving FLB recovery points remain recoverable.
