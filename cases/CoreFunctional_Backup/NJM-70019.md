# NJM-70019 — [FLB v1] FLB - Functional - Use Case: Create and Run a Basic File/Folder Backup Job (UC1 & UC2)

- **Source TC:** NJM-70019 (Xray, FLB/Share, Blocker P1, flb-v1; linked closed defect NJM-99220)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-70019, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE

## Objective
Verify UC1/UC2: create a File-Level Backup job selecting files/folders, run it, and confirm it
completes with a recovery point containing the selected items (spec: core create+run flow; Retention
step offers immutability only when an immutable repo is selected).

## Fixtures used
- Source: windows-src **PM-3** (or PM-2)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-70019`

## Preconditions
- [ ] Source present & OK — **R1**; Onboard repo online — **R3**; source items exist.
- [ ] **BLOCKED guard:** if source/repo not OK → BLOCKED(env).

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder select a mix of a folder + a file, `targetStorageVid=BACKUP_REPOSITORY-2`, ScheduleBuilder `schedules=[]` (on-demand), name `AUTO_FLB_NJM-70019` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** `lrState:OK` ("Last run successful").
### Step 4 — Verify — R7 · getSavepoints ≥1; FLR browse shows the selected items present.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if the job is created, runs to `lrState:OK`, and the recovery point contains the selected files/folders.
