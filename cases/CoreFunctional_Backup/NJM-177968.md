# NJM-177968 — [FLB] Reliability - Backup repository out of space fails job with error135

- **Source TC:** NJM-177968 (Xray, FLB/Share, Blocker P1, label FLB-Share-reliability)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-177968, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED / manual — needs a deliberately space-constrained repository.

## Objective
Verify that when the target repository **runs out of space** during a run, the job **fails with
error135** ("backup repository is out of space") — no silent hang, alarm clear and immediately visible
(spec/reliability).

## Fixtures used
- Source: windows-src **PM-3** with a dataset larger than free space
- Repository: a **space-constrained** repo `BACKUP_REPOSITORY-<id>` *(REQUIRED PREREQUISITE — not present)*
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-177968`

## Preconditions
- [ ] **BLOCKED guard (decisive):** requires a repository whose free space is smaller than the backup
      set (so it fills mid-run). No such constrained repo exists on nbr-84 → **BLOCKED(env/manual):
      "requires a space-constrained backup repository"** and stop.
- [ ] PM-3 present & OK — **R1**

## Steps (once a constrained repo exists)
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder a dataset > repo free space, `targetStorageVid`=constrained repo VID, name `AUTO_FLB_NJM-177968` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`).
### Step 3 — Poll — R6 · **Expect:** job **FAILS** with **error135** (out of space); no hang; alarm visible.

## Cleanup
- Job FAILED by design → keep + record job id. Remove when done: `remove [<jobId>, false]`.

## Expected final verdict
PASS if the run fails with **error135** promptly (no silent hang) with a clear alarm.
Otherwise **BLOCKED(env/manual)** until a space-constrained repository is provisioned.
