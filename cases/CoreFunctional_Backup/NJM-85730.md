# NJM-85730 — [FLB v1] FLB - Repository - Verify Self-Healing Functionality for FLB Backups

- **Source TC:** NJM-85730 (Xray, FLB/Share, Critical P2, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-85730, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — needs the repository self-healing/verification RPC (introspect); else UI/manual.

## Objective
Verify repository **self-healing/verification** runs successfully over FLB backups and that FLB
recovery points remain valid afterward (spec/repository maintenance).

## Fixtures used
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`) or **NFS_REPO** (`-7`) holding FLB full+incremental RPs
- Source: windows-src **PM-3** (to seed the FLB backup)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-85730`

## Preconditions
- [ ] Repo online — **R3**; ≥1 FLB job with full+incremental RPs (build via R4c if absent).
- [ ] **BLOCKED guard:** introspect for the self-heal RPC — `describe_method` on `BackupManagement`/
      `RepositoryManagement` (e.g. runSelfHeal/verify/runVerification). If no self-heal RPC is exposed
      → mark **PARTIAL**: perform via the UI (Repository → Manage → Run self-healing/verification) and
      screenshot (R8).

## Steps
### Step 1 — Ensure FLB backup — R4c create+run (full), add data, incremental → `getSavepoints` shows the chain.
### Step 2 — Run self-healing — call the introspected self-heal/verification RPC on the repo (or UI path).
### Step 3 — Wait — self-healing completes with **no errors** for FLB backups.
### Step 4 — Verify — R7 · FLR from an FLB RP after self-healing → recovery succeeds (backups valid).

## Cleanup
- On PASS — **R9** remove the AUTO_FLB job `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if self-healing completes with no errors and FLB recovery still succeeds afterward.
