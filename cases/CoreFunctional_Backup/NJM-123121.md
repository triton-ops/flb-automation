# NJM-123121 — [FLB v1] FLB - Functional - Verify Backup to Wasabi Repository (Windows & Linux)

- **Source TC:** NJM-123121 (Xray, FLB/Share, Critical P2, label flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123121, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (Wasabi_Repo id 6 present on nbr-84)

## Objective
Verify FLB backs up mixed test files from Windows **and** Linux sources to a **Wasabi** repository
across a full + incremental run, and that FLR-recovered files are byte-identical to source
(spec: Wasabi is a supported S3-compatible immutable-capable destination).

## Fixtures used
- Sources: windows-src **PM-3** (`C:\TestData_ForFLB`) and linux-src **PM-2** (`/TestData_ForFLB`)
- Repository: **Wasabi_Repo** (`BACKUP_REPOSITORY-6`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-123121`

## Preconditions
- [ ] PM-2 and PM-3 present & OK — **R1**
- [ ] Wasabi repo id 6 online — **R3**
- [ ] Checksums recorded (manifest-windows.sha256 / manifest-linux.sha256)
- [ ] **BLOCKED guard:** if the Wasabi repo is absent/offline → STOP, report BLOCKED(env).

## Steps
### Step 1 — Build the job — R4c
- SourceBuilder: two objects — one `sourceVid=PM-3`/`Windown`, one `sourceVid=PM-2`/`Linux_16.84`
  (duplicate `objects[0]` in the template for the 2nd source). MappingBuilder: a folder on each
  (`C:/TestData_ForFLB/ft_code`, `/TestData_ForFLB/ft_code`). RepositoryBuilder:
  `targetStorageVid=BACKUP_REPOSITORY-6`. Name `AUTO_FLB_NJM-123121` → `saveJob`.
- **Expect:** `{OK, jobId}`.
### Step 2 — Full run — R5 (`runType:ALL`) · **Expect:** accepted.
### Step 3 — Poll — R6 · **Expect:** `lrState:OK`, `lrVmOk=2`.
### Step 4 — Incremental — add data on both sources (remoting) → run again · **Expect:** new RP, `lrState:OK`.
### Step 5 — Verify — R7 · FLR export from a Wasabi RP → SHA-256 vs manifests (both hosts). Cloud RPs are slow — poll patiently.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if full+incremental reach `lrState:OK` (lrVmOk=2) and recovered files match both manifests.
