# NJM-130057 — [FLB v1] FLB - Functional - Verify Job Execution with Repository Encryption Enabled/Disabled

- **Source TC:** NJM-130057 (Xray, FLB/Share, Critical P2, flb-v1)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-130057, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ✅ RUNNABLE (needs an encryption password configured on nbr-84; else BLOCKED for the encrypted job).

## Objective
Verify FLB runs with **backup encryption disabled (Job A)** and **enabled (Job B)**, both complete
successfully, and files recover from the encrypted backup (spec: Options-step Backup Encryption;
encryption must not corrupt/block recovery).

## Fixtures used
- Source: windows-src **PM-3** (test files .pdf/.docx/.jpg)
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job names: `AUTO_FLB_NJM-130057_A` (enc off), `AUTO_FLB_NJM-130057_B` (enc on)

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**
- [ ] **BLOCKED guard:** Job B needs a configured **encryption password** (an encryptionPasswordId).
      If none exists on nbr-84 → run Job A (PASS) and mark Job B **BLOCKED(env): "requires an
      encryption password"**.

## Steps
### Step 1 — Job A (encryption OFF) — R4c: `sourceVid=PM-3`, folder mapping, `options.backupEncryptionMode="NONE"`, name `AUTO_FLB_NJM-130057_A` → `saveJob` → run → **Expect:** `lrState:OK`.
### Step 2 — Job B (encryption ON) — R4c: same source/folder, BackupOptionsBuilder `options.backupEncryptionMode` = enabled + `options.encryptionPasswordId=<id>`, name `AUTO_FLB_NJM-130057_B` → `saveJob` → run → **Expect:** `lrState:OK`; RPs stored encrypted.
### Step 3 — Verify — R7 · FLR-recover from Job B's encrypted RP → EXPORT→SHA-256 vs `manifest-windows.sha256` (encryption does not block/corrupt recovery).

## Cleanup
- On PASS — **R9** remove both jobs `remove [<id>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if both jobs reach `lrState:OK` and the encrypted job's files recover byte-identically.
