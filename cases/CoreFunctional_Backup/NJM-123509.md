# NJM-123509 — [FLB v1] FLB - Functional - Verify Backup with Repository Encryption Enabled

- **Source TC:** NJM-123509 (Xray, FLB/Share, Critical P2, flb-v1; related closed NJM-123512, NJM-128541)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-123509, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** ⛔ BLOCKED(env) — no **encryption-enabled repository** on nbr-84.

## Objective
Verify FLB backs up to a **repository created with encryption enabled**, RPs are stored encrypted,
and files recover with integrity (spec: repository-level encryption; recovery unaffected).

## Fixtures used
- Source: windows-src **PM-3** (test files .pdf/.docx/.jpg)
- Repository: an **encryption-enabled** repo `BACKUP_REPOSITORY-<id>` *(REQUIRED PREREQUISITE — not present)*
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-123509`

## Preconditions
- [ ] **BLOCKED guard (decisive):** `getBackupRepositories` must contain a repo with **encryption=true**,
      state OK. None on nbr-84 today → **BLOCKED(env): "requires an encryption-enabled repository"** and stop.
      (Note: this is REPO-level encryption; job-level backup encryption is covered by NJM-130057.)
- [ ] PM-3 present & OK — **R1**

## Steps (once an encrypted repo exists)
### Step 1 — Build — R4c: `sourceVid=PM-3`, folder mapping, `targetStorageVid`=encrypted repo VID, name `AUTO_FLB_NJM-123509` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** `lrState:OK`; RPs stored encrypted.
### Step 3 — Verify — R7 · FLR-recover → EXPORT→SHA-256 vs `manifest-windows.sha256` (encryption transparent to recovery).

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL/BLOCKED — keep + record.

## Expected final verdict
PASS if `lrState:OK` and files recover byte-identically from the encrypted repo.
Otherwise **BLOCKED(env)** until an encryption-enabled repository is provisioned.
