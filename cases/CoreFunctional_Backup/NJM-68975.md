# NJM-68975 — [FLB v2] FLB - Functional - Verify System Files (e.g., pagefile.sys) are Skipped

- **Source TC:** NJM-68975 (Xray, FLB/Share, Major P3, flb-v2; linked closed defect NJM-134346)
- **Feature:** File-Level Backup (physical machine)
- **Appliance:** nbr-84 (FLB) — see `test-data/environment.md`
- **Metadata:** testcase_id=NJM-68975, author=tri.ton, date=2026-07-07, product=FLB, status=GENERATED
- **Feasibility:** 🟡 PARTIAL — needs a Windows scope that includes system files; verify via job log + FLR.

## Objective
Verify Windows **system files** (`pagefile.sys`, `hiberfil.sys`, `swapfile.sys`) are **skipped** (not
errors) while ordinary files back up; the RP excludes system files (spec: system folders unselectable/
not backed up; skip-and-continue).

## Fixtures used
- Source: windows-src **PM-3** — a volume/folder scope containing system files + ordinary files
- Repository: **Onboard** (`BACKUP_REPOSITORY-2`)
- Job build: **R4c**; Job name: `AUTO_FLB_NJM-68975`

## Preconditions
- [ ] PM-3 present & OK — **R1**; Onboard repo online — **R3**
- [ ] **Setup:** scope must include system files (e.g. select `C:/` volume root or a folder holding
      `pagefile.sys`). Confirm ordinary test files coexist (e.g. `C:/TestData_ForFLB/…`).
- [ ] **BLOCKED guard:** if the scope cannot include system files → PARTIAL/BLOCKED, note why.

## Steps
### Step 1 — Build — R4c: `sourceVid=PM-3`, MappingBuilder a scope containing system + ordinary files, `targetStorageVid=BACKUP_REPOSITORY-2`, name `AUTO_FLB_NJM-68975` → `saveJob`.
### Step 2 — Run — R5 (`runType:ALL`) · **Expect:** `lrState:OK` (job not failed by system files).
### Step 3 — Skip check — inspect `ActivityManagement`/job events → `pagefile.sys`/`hiberfil.sys` listed **skipped** (not errors).
### Step 4 — Verify — R7 · FLR `list` shows system files **absent** from the RP; ordinary files present; EXPORT→SHA-256 vs manifest for ordinary files.

## Cleanup
- On PASS — **R9** `remove [<jobId>, false]`. On FAIL — keep + record.

## Expected final verdict
PASS if `lrState:OK`, system files reported skipped and absent from the RP, ordinary files recover intact.
