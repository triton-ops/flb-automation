# Dev/CI tooling on top of the runtime deps. Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.0.3
ruff==0.6.9
# Emits Allure results for the pytest run itself (via --alluredir), so THIS run's own unit-test
# outcomes appear in the Allure report alongside the reporting/ layer's journal-based results
# (which cover recorded NBR runbook executions) — same output directory, both sources merge.
allure-pytest==2.16.0
