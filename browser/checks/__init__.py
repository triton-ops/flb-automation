"""Runnable UI-validation checks (one module per TC).

Each check navigates via the POM (../pom), screenshots the decision point into
../../results/screenshots/<TC>/, and prints the signals the vision step reads.
"""
