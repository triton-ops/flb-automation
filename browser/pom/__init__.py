"""Page Object Model for NBR Director UI checks (Option C: Playwright + vision).

Page objects own their selectors + actions (the single place to maintain when the UI
changes). Test data lives in ../config/ui_values.json; secrets in ../config/ui_config.json.
"""
